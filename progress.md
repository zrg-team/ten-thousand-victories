Original prompt: Please implement the proposed Vạn Thắng documentation and Phaser 3 TypeScript mobile-vertical MVP plan.

## 2026-09-07 — raise Auto fallback to 40 FPS

- User requested 40 FPS instead of the 30 FPS minimum target. Clarity now targets 40; the normal target stays 60. Preserved buffer/ground resolution, decoration density, labels, manual choices, and fixed-session behavior. Explicit legacy low-30 remains a diagnostic profile only.
- Generalized target selection to honor sub-60 profiles, bumped automatic calibration version to 5, and adjusted the shared preparation allowance to 4.5 ms at 40 FPS (same 18% share of frame time). The existing elapsed-time pacer needs no algorithm change.
- At the user's localhost:5179 URL, headed AMD 8060S Chromium with 4× CPU throttling selected Clarity/40 on launch. Fully revealed real mouse drags measured 39.94 and 40.02 FPS, p95 CPU 25.4 and 22.3 ms, zero browser tasks over 50 ms. The 2702×1520 buffer and 2,933 visible decorations were unchanged. Target and profile remained fixed. These are short CPU-stress samples, not a guaranteed hardware minimum.
- Validation passed: all 12 combinations of 30/40/60 FPS and 60/90/120/144 Hz preserved exactly 60 seconds of simulation; preparation budget fairness passed at 40 FPS and legacy/normal/full-refresh targets; FPS-cap integration 13/13; launch/manual policy 10/10; production build with existing font-path and bundle-size warnings. Installed skill client completed two gameplay bursts on Clarity/40; screenshot and text state inspected, no browser errors.
- Artifacts: output/map-drag/auto-40-cpu4x.json and screenshot, output/map-drag/skill-40/client/. Updated performance docs with the new target and measured result; retained historical 30 FPS measurements. Existing unrelated changes preserved. No required work remains; reload with Auto enabled to run the updated profile selection.

## 2026-09-07 — desktop menu, split controls, notification spacing

- User requested menu optimization, separate pause/menu buttons, and more desktop space between notifications and the header. Proceeded with the stated right-panel menu layout after offering a centered alternative as an optional preference.
- Desktop front page now uses one compact, nearly opaque parchment panel on the right, with the existing wordmark inside. The full landscape is visible beside it; the old translucent center-column veil is hidden only on the desktop main page. Continue is the primary action when a save exists, with its in-game year/season instead of a long locale timestamp. New Game is a separate secondary action. Dynasty, Classic, utility/language controls, and support/version details follow a consistent vertical layout. The phone front page retains its existing arrangement.
- Split the header pair into independent quiet paper buttons, retaining 44-unit targets, the twelve-unit visual gap, tooltips and input guards. Removed the shared-tray implementation. Desktop advice now starts ten design units below the header; its published tap bounds and following whispers move with it.
- Updated the install mark to center around the actual version-line position. A real hover check exposed the shared press animation restoring pre-layout coordinates on a hover-only exit; it now settles only after a press. The failing hover audit was reproduced in all three desktop sizes, then passed after the fix.
- Validation passes: desktop/phone menu 31/31; new-run/manual-save/install checks 7/7; gameplay/layout checks 65/65; existing phone utility/language/install suite 17/17. New Game starts a hands-on run and preserves the manual save; Continue resumes the saved year. Production build and TypeScript pass with the existing font-path and bundle-size warnings. Inspected fresh/saved wide and narrow desktop menus, phone screens, separate controls, notification placement, plus final installed-client gameplay screenshot and text state; no browser errors.
- Review: `output/desktop-menu-polish/` (notably `wide-saved.png` and `gameplay/wide-desktop-detail.png`). Scratch drivers are `_desktop-menu-polish.mjs`, `_desktop-new-game.mjs`, and `_split-gameplay-controls.mjs` under `test_scripts/scratch/`. Preserved unrelated working-tree changes. No required work remains.

## 2026-09-07 — desktop map dragging at localhost:5179

- User requested implementation and verification of lag while dragging the map at the running localhost:5179 server. Reproduced on a fully revealed 1920×1080 High map with 4× CPU throttling in headed Chromium using the actual AMD 8060S GPU.
- Fixed Phaser Rectangle edge-getter loss in ViewIndex, retained/rebound object visibility, and the ground-bake path revealing previously culled artwork. Spatial queries now reuse object records and numeric visit stamps. Ownership washes retain individual province Graphics, cull offscreen provinces, and preserve paint order when a province returns.
- Strengthened Auto launch calibration to approximately 4,300 images plus contour/list work at desktop width, reserving CPU headroom for the full scene. Profile version 4 refreshes automatic records; manual choices, source artwork, buffer clarity, and fixed-session settings are preserved. Extra custom image/geometry-cache experiments were removed after their measurements failed to justify them.
- Final normal Auto chose High and measured 59.2–60.0 FPS while dragging. With 4× CPU throttling from launch, Auto chose Clarity in 1,902 ms and held 30.0 FPS on the revealed-map drag, p95 CPU 27.1 ms, no browser tasks over 50 ms in 12.8 seconds. Both kept the 2702×1520 drawing buffer. Manual High remains about 26 FPS under this stress; its geometry upload fell about 15%. These isolated, paused-world measurements do not certify all desktops or packaged builds.
- Validation: drag/season/ownership/paint-order/zoom-limit/resize regression passes across Low/Medium/High, English/Vietnamese, and 1280/1920/3440px desktops. 30,000 spatial queries match a brute-force oracle; 30/60 FPS clock tests pass on simulated 60/90/120/144 Hz. Launch policy passes 10/10 and resume/context/save recovery passes 21/21. Production build passes with existing font-path and bundle-size warnings. Required skill client completed two gameplay bursts; screenshot and text state reviewed with no game errors. Its first probe hit an intentionally blocked Vite socket and non-preserved canvas readback; the final probe used normal HMR and the existing capture flag.
- Added repeatable `perf:drag` and `verify:map-drag` scripts and `docs/development/map-drag-performance.md`. Measurements/screenshots: `output/map-drag/` and `output/verify-map-drag/`. Preserved unrelated concurrent changes. To exercise the new automatic choice in the user's browser, select Auto and reload.

## 2026-09-07 — quieter header controls

- User requested another refinement of the pause/menu pair in a close-up screenshot. The desktop pair now shares one fine paper outline and a short central divider. Removed the individual borders, corner brackets, shadows, and redundant separator beside/below the header cluster. Resume keeps a pale vermilion wash; menu strokes are equal in length. Both icons sit on the header's centre line with equal clearance from its decorative bands.
- Preserved independent 44-unit targets, immediate single-press actions, localized hover labels, and keyboard shortcuts. Header readout width now reserves the actual control footprint, including at the minimum 780-unit desktop width. Phone controls retain their compact layout.
- Production build and TypeScript pass with the existing font-path/bundle-size warnings. The scratch gameplay check passes 62/62 across three desktop sizes and two phones, including frieze clearance, target separation, press/release behavior, menu and map interactions, shortcuts, and no browser errors. Installed game client completed three gameplay bursts; final screenshot and text state inspected. Visually reviewed the paused/running close-ups and minimum-width header.
- Review: `output/header-controls/wide-desktop-detail.png`; full screenshots, checks, and client output are in `output/header-controls/`. Scratch driver: `test_scripts/scratch/_header-controls.mjs`. Existing unrelated work was preserved. No required follow-up remains.

## 2026-09-07 — gameplay control styling

- User request: improve the gameplay pause/menu and zoom/map-view buttons shown in the attached screenshot, making them consistent with the game style.
- Added a shared cached paper control in `src/ui/GameplayControl.ts`: clipped paper corners, fine irregular ink borders, inset corner marks, restrained pigment shadows, matching icon weights, and distinct hover/pressed/active surfaces. Replaced the map-view colour blocks with a folded-map icon and a terrain illustration. Labels are localized in English and Vietnamese; desktop clock/menu hints include the existing shortcuts.
- The Ascent map controls and ActionBar system controls use the shared treatment. Zoom controls have separate 44-unit hit targets at a 48-unit pitch; the desktop header pair also has separate 44-unit targets. Phone system controls retain the compact bar layout with hit areas capped to avoid overlapping each other. Controls act once on press, with existing input-generation echo protection and paper tap feedback. Static art is baked; hover labels are created only when needed.
- Validation: TypeScript and the production/service-worker build pass (existing font-path and bundle-size warnings). Real pointer checks pass 44/44 at 1280×720, 1920×1080, 390×844 and 320×568: press/release zoom, unchanged land selection, both map views, pause/resume, menu visibility, desktop Escape/Space, and no browser errors. Visually inspected desktop, both phones, hover and territory-view captures. The first probe was interrupted by a live source reload; the complete reruns passed.
- Evidence and scratch harness: `output/gameplay-controls/` and `test_scripts/scratch/_gameplay-controls.mjs`. The installed game client completed three gameplay bursts through a temporary fixture; final screenshot/text state were inspected and no console errors were recorded (`client-final/`). Existing unrelated changes were preserved. No required styling work remains.

## 2026-09-07 — title correction after rejected modern lettering

- User said the red outlined title looked modern rather than Đông Hồ. Replaced the slab-serif construction with built-in ImageGen artwork: handwritten “Vạn Thắng”, broad soot-black strokes, single-storey lowercase forms and dry ink edges. It is described as a contemporary folk-inspired Quốc ngữ adaptation, not historical lettering. UNESCO craft reference, exact prompts, rejected first draft and source provenance are in `docs/design/dongho-wordmark-v2/prompt.md`.
- Added `public/art/menu-wordmark-dongho-v2.png` (1088×321, 274,530 bytes) and preloaded it. Simplified `dongHoWordmark.ts` to one image, reduced its width, and retained heading metadata and the quiet subtitle. The generator baked a checkerboard into its first selected lettering output; a built-in background edit replaced it with white. Multiply blending prints the selected master onto the menu's paper without a white rectangle. Asset preparation only trims/pads/resizes.
- TypeScript and production build pass with existing font/chunk warnings; service worker includes the new asset. Required three-burst game client reached the menu without errors; screenshot and text state inspected under `output/dongho-wordmark-v2/client/`. Reviewed 320×568, 390×620 and 390×844 screenshots with correct accents and no clipping. Real Play and saved Continue taps pass without browser exceptions. Task-file whitespace check passes.
- Preserved concurrent desktop/game changes. No required title-correction work remains; user can review `output/dongho-wordmark-v2/menu-vi-844.png`.

## 2026-09-07 — Đông Hồ-inspired game lettering

- User asked for the game name to follow the Đông Hồ art style. Added original carved Latin glyphs in `src/ui/ink/dongHoWordmark.ts`: vermilion fill, dark outlines, a restrained ochre registration edge and sparse print grain. Vietnamese breve, acute and underdot are explicit shapes. This is an inspired contemporary wordmark, not a claimed historical typeface.
- Main menu uses one cached 3× canvas texture, responsive width and the existing quiet English subtitle. No external font or raster download. Preserved menu controls and concurrent work. Heading metadata keeps the name available to existing visual checks.
- TypeScript and production build pass with existing font/chunk warnings. Utility/language checks: 17/17. Actual Play and saved Continue taps pass without browser exceptions. Inspected 320×568, 390×620 and 390×844 captures, plus required game-client screenshots/text state. Final accent-padding correction reviewed under `output/dongho-wordmark/client-ready/`; the final client reached the menu without errors after a temporary concurrent desktop-key export interruption and preload delay.
- Both living-map menu passes confirm the heading clears the landscape at 844px and 664px. The broader suite lost `window.__phaserGame` during later map sampling on two runs amid concurrent edits; full suite completion is not claimed. No title/layout assertion failed. Task-file whitespace check passes; no required wordmark work remains.

## 2026-09-07 — cleaner main menu without the app icon

- User approved removing the large main-menu app icon. Replaced its masthead with a compact VẠN THẮNG wordmark and quiet subtitle, moved the landscape up, brought Play higher, and grouped secondary navigation and language controls. Removed the unused emblem preload; launcher and splash assets remain available.
- Play retains at least a 48px touch target and saved games get a full 44px Continue row. Existing Dynasty, Classic, utility and language actions remain wired. Preserved concurrent desktop-layout and other scene changes.
- TypeScript and production build pass (existing font-path/chunk-size warnings). Utility/language suite: 17/17. Menu-fit suite: all 24 theme/language/height combinations pass. Living-map suite passes with no console errors; replaced its obsolete app-emblem assertion with the compact-heading/no-emblem expectation. The first fit run was interrupted by concurrent HMR; complete rerun passed.
- Required game-client screenshot and text state inspected. Final captures: `output/clean-menu/menu-vi-844.png` and `saved-620.png`. Real taps verified Play opens Dragon Ascent and Continue resumes a saved game on a short screen, without browser exceptions. Task-file whitespace check passes. No required menu work remains.

## 2026-09-06 — completed buildings and mountain render order

- User reported houses drawing over nearer mountains. Reproduced in a populated Dragon Ascent map: 373 of 407 structures (340 improvements and 33 enclosures) escaped the terrain band because harvest only recognized the core settlement image. They remained in depth-2 containers above all mountains.
- Harvest now includes satellites and enclosures, each at its own measured ink base. Procedural improvements carry their local ground anchor. Roads and capital washes are baked beneath standing objects. Existing moving sprites and unrelated working-tree edits are preserved.
- Added `test_scripts/verify/verify-structure-depth.mjs`; confirmed failure before the fix in `output/structure-depth/before/audit.json`. All three quality tiers now pass with 407 structures, 149 overlapping building/relief pairs and zero inversions. Seasonal and building rebuilds preserve the count and destroy old scene-level images. Medium/high passed 2,040 edge-of-viewport visibility checks across three zooms; low keeps the structures baked.
- Settlement culling uses the actual layout bounds, so harvested additions stay visible when their province centre is offscreen. No additional textures or per-frame depth recalculation were added.
- TypeScript, production Vite/service-worker build, existing ground-order checks (5/5), and all 11 settlement layouts pass. Existing font-path/chunk-size build warnings remain. Inspected low/medium/high overlap captures plus final installed game-client gameplay screenshot and text state under `output/structure-depth/`; no browser errors. The first baseline screenshot was obscured by coronation, so the baseline evidence is the failing audit, not that screenshot.
- No required render-order work remains. Existing unrelated edits and older progress-file whitespace are preserved.

## 2026-09-06 — hero headwear and creator fit correction

- Addressed user screenshots showing fixed-width hats on differently sized heads and a crushed chest sash. Added measured forehead/contact-band fitting for all 16 heads and 46 supported v2 hats. Phốc đầu uses separate crown/wing slices inside the existing cached portrait; closed headwear suppresses crown buns/ornaments. Hair/features follow head width; each ear/earring moves independently. Kerchiefs and cheek guards received separate clearance corrections after the expanded tests found eye overlap.
- Re-exported all seven sashes from the existing reviewed ImageGen master into taller canvases, moved them to the lower torso and removed competing second belts. No new historical costume form or reference was introduced. Both older packs and all 156 accepted anatomy/hair PNGs remain unchanged. Creator garment captions now describe the rendered outfit.
- Passed production build, all three art-pack/cache/saved-look/gameplay checks, 367 outfit samples, 10,304 exhaustive head/hat/eye pixel fits, 53,232 creator combinations, real creator face-grid selection, and the required game-client smoke loop. Inspected all-hat sheets for slim/oval/full heads, actual creator screenshots, the mixed roster gallery and smoke screenshot/state. Existing font-path and bundle-size build warnings remain.
- Review: `output/hero-dongho-v2/fit/` includes before/after hat sheets, three creator captures and test reports. `npm run verify:hero-fit-v2` reruns the creator audit; `npm run verify:wardrobe-v2` now includes the full head/hat/eye matrix. Updated pack is 2048×1636, 12.78 MiB decoded; completed heroes still use one cached Image. No required fit work remains.

## 2026-09-06 — Cờ Hiệu banner editor and Vietnamese references

- User asked to strengthen this page's weak feature icons and research Vietnamese “Co Hieu”. Understood from the screenshot as cờ hiệu (standards), not a kingdom name. Research and source limitations: `docs/research/vietnamese-co-hieu.md`; museum Ngọc Lũ catalogue, Nguyễn Kỳ Đài signal use, Tiên Công procession reporting and a contemporary Ngô lineage discussion. No unverified medieval flag or house-colour mapping presented as historical fact.
- Replaced the shield-shaped banner with a square cloth standard, pole, layered seams and flame-cut fringe. Added six dedicated code-native devices using the existing pigment palette: fourteen-ray bronze drum, command standard, sword, drooping rice panicles, bamboo and turtle. The old saved `crown` slot now displays the drum; all saved IDs/colour values/unlocks remain compatible. Tactical CardIcons and army-map flags stay on their existing paths.
- Enlarged the preview, named all colours/motifs, added ticks and visible lock requirements, and replaced the home masthead with a compact editor heading. Removed the misleading Ngô/Đinh army-style sentence from this page. Temple save now says “Giữ thay đổi” / “Save changes”. Swatches retain their exact colours; separating seams and contrasting motif ink handle matching field/trim choices.
- Both Temple and coronation preserve scroll on banner edits; text state reports the current banner and earned choices. Save/reload, discard, colour selection, every emblem, locks/unlocks, drag-without-selection and both hosts verified in `test_scripts/verify/verify-banner-editor.mjs`: **20/20**, no browser errors. Reviewed Vietnamese 390×844, 390×620, 320×568, English 390×844, the short coronation, Dynasty rendering, and the 30-combination colour sheet under `output/banner-editor/`.
- Final production build passes with the existing font-path/chunk-size warnings. Required web-game client ran after the final rendering changes; final screenshots and text state inspected under `output/banner-editor/client-final/`. Task-file diff check passes. Older `verify-coronation.mjs` is **22/23**: its remaining assertion expects `mandate` directly after the opening, while unchanged GameState now queues `inheritance` first. The gate does skip repeated coronations; all its rendering, wardrobe and completion checks pass. Left that unrelated assertion unchanged.
- No required banner work remains. Other concurrent icon, portrait, map and MenuScene app-emblem changes were preserved.

## 2026-09-06 — remaining rice farmer walk correction

- User approved the traveller improvements and asked to improve only the remaining figure in their screenshot. Matched it to the red-shirted rice-carrying farmer, not the hoe-carrying traveller. Used built-in ImageGen to lower the farmer's passing feet and knee while preserving its look.
- Replaced only `public/art/conquest-dongho-v4/life/farmer-walk.png`. Kept the 627px frame grid, 550px maximum subject height, and existing anchors/baselines; no runtime movement or size changes. SHA-256 comparison confirms the other 24 living-map assets, including the farmer still and all six traveller looks, are unchanged.
- Extended the existing pose checks to the farmer. The old fourth pose fails with 45px foot clearance; the new pose passes at 14px (69% lower). All 49 sprite checks pass. Actual Conquest verification finds 30 farmers with four distance-driven poses, stable height/baseline, facing and pause. The existing 62-road/six-look regression suite passes with no browser errors.
- Production build passes with existing font-path and bundle-size warnings. Installed web-game client ran two bursts; its state/screenshot and actual map captures were inspected. Before/after static and interactive review: `output/farmer-low-step/before-after.png` and `walk-comparison.html`; selected master, original backup and runtime audits are in the same directory. Prompt/provenance: `docs/art/conquest-farmer-low-step.json`.
- No remaining work for this targeted correction; preserved unrelated concurrent edits.

## 2026-09-06 — complete Đông Hồ hero wardrobe v2

- User rejected crude wardrobe geometry, especially `hat-band.png`, and required Vietnamese historical research before generation. Read additional museum garment catalogue descriptions and inspected actual Nguyễn garment photographs; read targeted armour/cap sections of *Ngàn năm áo mũ*. Evidence and limitations are in `docs/research/vietnamese-wardrobe-v2.md`. No Chinese costume reference substituted for Vietnamese clothing.
- Redrew all 140 wardrobe/headwear/accessory IDs with built-in ImageGen. New `public/faces-dongho-v2/` has 296 PNGs, 272 of generated origin; all 156 non-wardrobe resources remain byte-identical to v1. Both earlier packs preserved. V2 is the default; `?heroArt=legacy|dongho-v1|dongho-v2` selects a version.
- Complete garment assemblies replace broken strip layering. Neck sockets, under-robes, headband knots, armour construction, nón profiles, pins and insignia were reviewed in assembled portraits. Fixed earring aspect ratios and regenerated hats whose straps crossed eyes. New presentation avoids automatic medieval Nguyễn armour, unsupported early badges and generic civil dragon badges. Exact early armour and ceremonial assignments remain qualified reconstructions.
- Runtime stays one atlas plus JSON and one cached image per hero. V2 atlas 2048×1622 (~12.67 MiB RGBA); source PNGs excluded from precaching. `npm run faces:dongho-v2` rebuilds from committed PNGs without generation. Prompts, selected masters, refinement instructions and usage are in `docs/hero-dongho-v2*`.
- Validation passed: production build; all three art-pack loaders, cache/saved-look/switching and current game/roster screens; 367 outfit samples and 198 distinct sampled hat/eye combinations; all 140 changed wardrobe cutouts without visible magenta; 156 unchanged inherited parts; v1's 35-part fit regression (3/3); web-game smoke client; small roster sheets and the corrected gallery visually inspected. Existing font-path/bundle-size build warnings remain. Unrelated map manifest whitespace is outside this change.
- Reviewed outputs: `output/hero-dongho-v2/review/`. The prior broad hero-suite opening-flow incompatibility remains documented in v1 notes, not claimed to pass. No required wardrobe work remains.

## 2026-09-06 — lower traveller foot lift

- User rejected the new walking animation because the feet/knees lift too high. Corrected all five new styles with built-in ImageGen edits; preserved their randomized appearances and existing movement timing.
- The first image revision lowered knees but repeated the same supporting foot; rejected it. The selected second pass uses the original worker's low, close-legged passing pose as a motion reference. Exact prompts and output IDs are documented under `docs/art/conquest-traveler-low-step*`.
- Repacked the five runtime sheets/stills in `public/art/conquest-travelers-v1/life/`, with matching metadata. Old PNGs retained under `output/traveler-low-step/before/`; selected masters in `output/traveler-low-step/reviewed-masters/`.
- Added a meaningful foot-clearance guard: all five earlier sheets fail; all replacements pass. Fourth-pose clearance drops from 77/100/57/42/52px to 9/20/10/9/13px for basket/fisher/merchant/pilgrim/woodcutter (75–88% lower). Support is now checked relative to each pose's two feet so short ankle-height steps do not require an arbitrary 40px horizontal displacement.
- Verified 47/47 walk-sheet checks, the actual 62-road Conquest variety/animation/pause/culling/fallback suite with no browser errors, and production build (existing font/chunk warnings). Required web-game client ran; its screenshot, current gameplay capture and before/after passing-pose contact sheet were inspected. Interactive and video comparisons: `output/traveler-low-step/walk-comparison.html` / `.webm`.
- No remaining low-foot-lift work. Other ongoing icon, portrait, formation and settlement edits were preserved.

## 2026-09-06 — correct unreal capital architecture and footprints

- User rejected Lý, Lê and Nguyễn drawings as unreal/oversized. Redrew those three with built-in ImageGen: removed giant curls/posts, stacked rear palace tiers and elevated connecting structures. Corrected Nguyễn's repeated front wing arches after visual inspection. Historical evidence and reconstruction limits remain documented.
- Selected v2 PNGs in `public/art/conquest-capitals-v2/settlement/` are integrated through the existing registry. Đinh/Trần art and every older version are preserved. All five capital widths now cap at 64 visible units; Lê height ceiling is 52. The three revised sprites render 13–29% shorter, with all five widths within 6%.
- Updated the existing capital scale guards to compare actual visible widths and footprint areas rather than forcing wide low structures to match a deep compound's height. Both dimensions have hard ceilings, and town/capital area hierarchy remains checked. Synced manifest/export metadata.
- Build, all 11 settlement layouts, and all four missing/corrupt-art fallback cases pass; 42 labels / 35 structures / zero overlaps. Visual scale 15/17: all settlement checks pass, existing farmer/traveler and stale 45-structure failures remain. Installed web-game client ran two bursts; screenshot and state inspected, no browser errors. Inspected all three dark-background exports, five-capital comparison and actual Lê/Nguyễn gameplay captures. Lý remains a gallery/runtime-metrics review because progression does not select it.
- Details/prompts/provenance: `docs/art/capital-redesign-v2*`. Comparison and map captures: `output/capital-redesign-v2/`. Legacy manifest has mixed indexed newlines; diff check passes treating CR as EOL. No remaining correction work; preserved concurrent unrelated edits.

## 2026-09-06 — historically informed capital redesign

- Follow-up request: keep the approved sizes, make each capital beautiful and recognizably royal, and research Vietnamese history first. Checked museum and heritage-centre sources; evidence, uncertainty and exclusions are recorded in `docs/research/capital-architecture-redesign.md`.
- Generated five new capital compounds with built-in ImageGen. Added layered royal halls, galleries and ceremonial gates at the existing 50–56-unit heights and 80-unit maximum width. Corrected Lê gate/stair counts and Nguyễn's Ngọ Môn passage orientation after visual review. Earlier palaces remain source-informed game interpretations, not exact archaeological reconstructions.
- Integrated only the five capital paths through `src/ui/conquestDongHoV4Assets.json`; originals in the v4 settlement folder are preserved. New transparent PNGs and export metadata are in `public/art/conquest-capitals-v1/`. Exact prompts, accepted/rejected source records and export details are in `docs/art/capital-redesign-v1*`. Exporter only handles matte removal, crop and uniform downsampling.
- Inspected all five on paper and dark backgrounds and at map scale. Comparison: `output/capital-redesign-v1/capital-comparison.png`. Actual ConquestScene captures cover Đinh, Trần, Lê and Nguyễn; Lý was reviewed with runtime metrics in the comparison because existing progression does not select Lý.
- Build passes (existing font/chunk warnings), all 11 layout cases pass, 42 labels / 35 structures / zero overlaps, and all four missing/corrupt-art fallback cases pass. Broader visual-scale suite remains 15/17 with its two pre-existing unrelated failures; all settlement checks pass. Installed web-game client completed two bursts with no browser errors; screenshots and text state inspected. No remaining capital-art work. Preserved concurrent unrelated changes.

## 2026-09-06 — consistent settlement and capital sizes

- Request: inspect every building in `public/art/conquest-dongho-v4/settlement` and make map scale consistent, especially oversized capitals. Reviewed all 11 cutouts against the unchanged standalone house and captured the real Dragon Ascent map.
- Adjusted the shared settlement size contracts: rural compounds 28–32 units, village/town/shrine 38–44, capitals 50–56 (previously 76–86). Capitals are 33–37% smaller; other settlements are 14–18% smaller. Added width limits for all 11 variants. Source PNGs and their proportions are preserved. Rendering, landscape clearance, roads, labels and satellite placement already consume this shared sizing.
- Synced production manifest metadata and its export helper. The helper now preserves line endings and tolerates absent, ignored review output. Updated the existing visual-scale assertions to require a restrained capital/town relationship rather than monumental scale.
- Verification: TypeScript and production build pass (existing font and bundle warnings). All 11 satellite layouts pass; 42 live labels avoid all 35 structures and each other. Visual-scale suite 15/17: settlement checks all pass; the same pre-existing farmer/traveller height mismatch and stale structural-asset count still fail. No new browser errors. Required installed web-game client ran two action bursts; gameplay screenshots and text state inspected.
- Review: `output/settlement-sizing/after-all-settlements.png` compares the original measured sizes with the adjusted sizes. `after-map-1.4.png`, `after-map-2.2.png` and `client/shot-0.png` show gameplay. Do not use `before-map-*` as before/after evidence: their attempted runtime metadata override did not change the renderer's cached module instance. No remaining settlement-sizing work. Concurrent portrait, army and traveller edits were preserved.

## 2026-09-06 — Conquest traveller variety

- Request: reduce duplicated travellers with five new styles and randomized appearances.
- Generated five matching four-pose Đông Hồ civilian sheets: basket carrier, fisherman, cloth merchant, pilgrim, and wood gatherer. Keep the original as a sixth option.
- Integrating stable seeded appearance selection for roads and settlement wanderers, with different looks within each road. Preserve existing gait, scale, speed, pause, and culling behavior.
- Runtime artwork is in `public/art/conquest-travelers-v1/life/` (five stills and five 2x2 walk sheets). Exact built-in ImageGen prompts and generation notes are in `docs/art/conquest-traveler-variants*`; the reproducible packer is `scripts/conquest-art/traveler-variants.mjs`.
- Integrated a seeded shuffle per road using map seed + road identity, selecting without replacement for sibling travellers; settlement people use the same pool. Existing objects and styles survive refresh, with original-art/still/procedural fallbacks preserved. New sheets share the existing 64px runtime reduction and distance-driven gait without extra bobbing.
- Verified: 42/42 asset checks; actual Conquest runtime across 62 busy roads includes all six looks with no same-road duplicates, varied settlement people, stable refresh, matching scale, four poses, facing, pause/cull/resume and missing-art fallbacks; zero browser errors. TypeScript and production build pass (existing font/chunk warnings). The required web-game client was run; its menu capture, the light/dark contact sheet and actual map captures at 1.2/2.4 zoom were inspected. Artifacts: `output/traveler-variants/`.
- Existing unrelated working-tree edits, including concurrent portrait, formation and settlement-scale work, are preserved. No remaining traveller TODOs.

## 2026-09-06 — versioned Đông Hồ hero PNG pack

- Implemented `public/faces-dongho-v1`: 296 individual transparent PNGs, 187 newly generated parts plus 109 explicitly documented legacy raster compatibility parts. The original `public/faces/` is untouched. All ten selected generation groups were inspected; hollow heads, a missing eye and incorrect teeth were regenerated. Pre-research drafts remain rejected.
- `FaceRenderer` uses the selected pack, includes its ID in badge cache keys, retains one cached image per hero, and omits unsupported marks/crowns plus the obsolete scalp highlight only for v1. Existing saved part lists remain unchanged.
- Switch with `?heroArt=legacy` / `?heroArt=dongho-v1`, build setting `VITE_HERO_FACE_ART_PACK`, or the persisted helper. Default is v1. Map selection stays independent.
- Individual PNGs are 3×; runtime atlas is 2× at 2048×1422 (~11.1 MiB RGBA vs legacy 16 MiB). PNG download is larger than the old SVG. Service-worker precaching excludes individual source PNGs to avoid duplication.
- `npm run faces:dongho` repacks committed PNGs without generated masters. Prompt set, selected tool-call log, provenance and research limitations are documented in `docs/hero-dongho-v1.md`.
- Dedicated `verify-face-art-packs.mjs` passed both loaders, all 296 part IDs, cache reuse, saved-look preview immutability, URL/persisted/default selection, and real game/roster screens without browser errors. Inspected legacy/new comparison captures and 160 roster/generated portraits. Web-game skill smoke client passed and screenshot was inspected. Production build passed with existing font/chunk warnings.
- Wider `verify-heroes.mjs` stops at `first.options[0]` in its coronation/opening-flow setup; do not report that suite as passing. Unrelated formation/map/figure/traveller edits remain untouched.
- Final follow-up: creator headwear uses the same frozen pack selection and excludes invisible crown choices. Final build and both-pack checks passed; all 3 portrait-fit checks passed for 35 worn parts, worst overhang 11 within budget 12. Confirmed service worker includes the PNG atlas and excludes individual source PNGs. Inspected final captures after scalp and padding corrections.

## 2026-09-06 — hero portrait art research checkpoint

- Current request: create beautiful Đông Hồ raster face-part resources, preserve `public/faces/`, and support independently selectable versions.
- User explicitly requires Vietnamese art, people, history, and wardrobe research first; no assumptions or Chinese costumes substituted for Vietnamese ones.
- Read the existing wardrobe research, visual-language guide, Đinh reconstruction dossier, and wardrobe resolver. Verified external museum/UNESCO material, read Kelley's review in full, and consulted targeted sections of *Ngàn năm áo mũ*. Source limits and acceptance rules are recorded in `docs/research/dong-ho-hero-art-direction.md`.
- The initial text-only hair/headwear/garment image sheets are rejected as production sources. They were not copied into the project or integrated. Verify existing silhouettes against dated evidence; do not alias many period-specific parts to a generic generated family.
- Expanded research and visual inspection of museum prints and dated clothing photographs are recorded in `docs/research/vietnamese-portrait-research-findings.md`. This identifies unsupported era fallbacks, film-derived armor, and facial-marking rules. Specific crowns, armor, early hairstyles, and monastic markings still need individual references before generation; this research pass generated no new images.
- Only an unused version-selection scaffold, `src/ui/faces/artPack.ts`, has been added so far. The existing renderer and `public/faces/` are unchanged. Implementation, source-constrained art generation, and runtime verification remain outstanding.
- Unrelated working-tree edits appeared in formations, map rendering, and ink devices during this task; do not overwrite them.

## 2026-09-05 — graphics assessment

- Reviewed fresh current-build captures of the menu, progressed Dragon Ascent, medium/high map rendering and zooms, portraits, decision and management screens, active arena combat, seasonal variants, Vietnamese History army plates, and desktop framing. No gameplay or artwork edits.
- Written assessment and evidence are in `output/graphics-review-2026-09-05/review.md`. Overall subjective rating: 6/10; strongest assets are the setting and landscape, weakest areas are portrait cohesion, map sharpness, and information hierarchy.
- Priorities for a later implementation task: crisp and compact map annotations; shared drawing rules across portraits/soldiers/environment/UI; expressive principal portraits; clearer battle silhouettes and consequences; illustrated historical decisions.
- Capture caveat: the copied promotional battle driver ended without an active fight. Its `battle.webp`/`armies.webp` are excluded from conclusions; confirmed arena captures supply battle evidence. All review capture runs reported no browser errors where errors were collected. Capture scripts are under `test_scripts/scratch/`.

## Progress
- Created a Vite + TypeScript + Phaser 3 scaffold.
- Added comprehensive game design and implementation docs under `docs/`.
- Added data-driven MVP game state, systems, scenes, and mobile UI helpers.
- Installed npm dependencies.
- `npm run build` passes with a Vite chunk-size warning from Phaser bundle size.
- Browser verification passed for visible portrait rendering, land selection, peaceful acquisition, hero recruitment, and army movement.
- Kept `Phaser.AUTO` and added `preserveDrawingBuffer` so Playwright screenshots render reliably.
- Improved UI/UX to a paper strategy style: province-area map, watercolor terrain, river/roads, settlement clusters, parchment bars, paper land panels, Tinder-style hero/politics cards, and large animated soldier formations.
- Browser verification passed for the new visual map, hero card stack, politics card, marching soldier animation, and polygon-area acquisition.
- Smoothed map readability by making roads/borders faint and adding clear terrain textures for rice fields, forest, mountains, open fields, cities, and river.
- Browser verification passed for the clearer texture map and province acquisition after the hit-area changes.
- Added a reusable procedural hero face renderer with deterministic random eyes, mouth, nose, beard, hair, hats, shirts, skin tones, hair colors, and outfit colors per hero id.
- Browser verification passed for the updated hero card portrait layout.
- Corrected the campaign model from turn-based to real-time: removed visible Orders/End controls, added automatic real-time progression, and deleted the stale TurnSystem.
- Enlarged the map world and added drag panning while keeping the UI vertical and fixed.
- Browser verification passed for enlarged map rendering, drag-panned map view, and automatic time/resource progression on localhost:5173.
- Replaced bottom-sheet hero/court/war interactions with paused full-screen gameplay screens: hero recruitment cards, court assignment, and army creation.
- Renamed the bottom `War` action to `Army`, added soldier allocation and led army creation from recruited heroes.
- Fixed dead/leaking UI taps by adding DOM-level canvas tap routing for bottom buttons, modal controls, and map province selection.
- Court events now appear as pending map requests instead of immediately opening/pause-stealing a card; opening the request pauses gameplay.
- Browser verification passed for Heroes tap, recruit flow, Court assignment screen, Army creation, and direct land selection on localhost:5173.
- Rebuilt `FaceRenderer` as a constrained layered portrait renderer: dark frame, centered face, fixed facial feature zones, restrained hats/body/props, and hero-type style pools to avoid unreadable overlaps.
- Adjusted `HeroDraftPanel` portrait scale/position so the older bottom-sheet card layout no longer lets the face overlap text.
- Browser verification passed for the new fullscreen hero recruitment portrait on localhost:5173.
- Simplified hero portraits further: removed portrait props, replaced angular split head with a centered oval face, moved the hat into a centered top-only zone, and reverified the fullscreen hero card screenshot.
- Fixed the remaining portrait anatomy issues: beard is now separate anchored left/right/chin pieces, shirt is a centered robe/collar layout, and generals always use the stable centered beard/hat silhouette.
- Locked hero portrait anatomy to one fixed centered template: one wrap hat, one goatee beard, one centered robe, no drifting face shading layer, and verified the full-screen hero card screenshot.
- Replaced the portrait renderer with a minimal symmetric template: fixed x=0 hat, face, goatee, and shirt; removed random geometry, side arms, robe split, and beard highlights that made placement read off-center.
- Reworked portrait coordinates to derive hat, beard, and shirt from `FACE_X`, `HEAD_TOP`, and `CHIN_Y`; replaced polygon point-cloud shirt with centered primitives and removed shoulder triangles after screenshot verification.
- Replaced the triangle hat/beard with a centered rounded cap, centered hat band, centered moustache, and centered goatee; verified the hero card screenshot on localhost.
- Adjusted the portrait again so the collar is a mirrored centered V, the cap/band sits closer to the head, and the rounded head is no longer cut flat by the hat.
- Added a hair layer under the hat with centered fringe and side hair, and moved collar rendering into a separate centered V layer drawn after the head so it is not hidden/skewed by shirt/head order.
- Added deterministic random face variants while preserving the fixed portrait anchors: head shape/size, eyes, nose, mouth, beard, collar, shirt, hat, skin, hair, beard, and outfit colors now vary per hero id.
- Reworked fog-of-war puffs into layered overlapping circle clusters (shadow, body, highlight passes) so unexplored districts read as fluffy clouds instead of flat tinted ellipses.
- Building construction is now a timed order: `buildDistrictBuilding` spends resources and queues a `BuildOrder` (2 economy ticks); `progressBuildOrders` advances/completes orders each tick and adds the building to the district.
- The land panel shows `Building <type>: progress/required` while a construction order is active, and the build modal blocks new orders on a district that already has one in progress.
- The map now shows a hammer-and-progress badge over districts under construction, and completed farm/mine/market buildings render small satellite icons (crop patch + cottage, mine mound, market stall) around the settlement.
- Verification: `npm run build` passes; Playwright check confirmed cloud-styled fog over hidden districts, the Build modal queuing a farm with a 0/2 progress order, and the farm decoration appearing on the capital after the order completed.

## TODO
- Implemented shared ink UI renderer pass: added `InkUI` primitives for buttons, panels, cards, modals, stat bars, and masked scroll areas; routed legacy panel/button helpers through the new renderer; migrated HUD chrome, court/build scroll lists, land/battle panels, army modal, court request, battle result, victory panel, and map overlay controls to the ink-scroll visual style.
- Fixed follow-up UI overlap issues: enlarged modal headers, shortened modal subtitles, converted Heroes roster to content-bound scroll cards, compacted the court request badge into a red seal, removed stale Build scroll code, trimmed bottom-sheet text, and removed the Army selected badge that overlapped portraits.
- Verification: `yarn build` passes; web-game Playwright client runs; targeted Playwright screenshots checked for Heroes, Court, Army, court badge, and selected-land bottom sheet with no browser console errors. Vite still reports the existing Phaser bundle-size warning.
- Screenshot artifacts: `output/web-game/ui-recheck-heroes.png`, `output/web-game/ui-recheck-court.png`, `output/web-game/ui-recheck-army-final.png`, `output/web-game/ui-recheck-court-badge.png`, and `output/web-game/ui-recheck-land.png`.
- Simplified the shared UI style after review: removed diagonal/ornamental panel treatment, flattened buttons/panels/cards, changed modal close to a simple `×` glyph with invisible hit area, and replaced the circular Court alert with a compact pill.
- Verification: `yarn build` passes; web-game Playwright client runs; targeted screenshots checked for the cleaned Heroes, Court, Army, and Court alert layouts with no browser console errors.
- Screenshot artifacts: `output/web-game/ui-clean-heroes.png`, `output/web-game/ui-clean-court.png`, `output/web-game/ui-clean-army.png`, and `output/web-game/ui-clean-court-badge.png`.
- Fixed generic text-flow issues: `InkUI.card` now stacks long row values vertically instead of two-column wrapping into neighboring rows; dense Build capacity cards and bottom-sheet cards have larger reserved heights; Build option rows were tightened so visible cards are complete rather than clipped through buttons.
- Verification: `yarn build` passes; web-game Playwright client runs; targeted Build and selected-land screenshots checked with no browser console errors.
- Screenshot artifacts: `output/web-game/ui-flow-build-final.png` and `output/web-game/ui-flow-land-final.png`.
- Future work: broaden automated gameplay checks for multi-turn battle capture and victory flow.
- Future work: split Phaser into a separate manual chunk if the Vite bundle-size warning becomes a release concern.
- Future work: replace generated paper/settlement drawings with final custom art assets once an art direction is locked.
- Future work: expose the face recipe in hero detail/debug UI if designers want manual overrides per named hero.
- Implemented expansion-race gameplay pass: larger 24x40 map, 25 districts, two non-neutral factions, one player capital, one rival capital, and 23 neutral districts.
- Replaced visible economy with food, supplies, gold, and humans; resource bar now shows stock plus net rate.
- Added tile-derived district capacity, terrain summaries, district outputs, and terrain-gated Farm/Mine/Market building actions.
- Updated neutral expansion: adjacent neutral land can be bought with gold, neutral/rival land can be conquered by adjacent armies, and victory triggers when the rival capital is captured.
- Updated AI to slowly claim/build frontier districts and disabled automatic politics/card interruptions for this expansion-focused pass.
- Verification: `npm run build` passes; Vite still reports the existing Phaser bundle-size warning.
- Verification: Playwright client generated `output/web-game/shot-1.png` and `state-1.json`; targeted browser state check confirmed two factions + neutral ownership, adjacent buy, non-adjacent buy block, farm build output increase, and rival-capital victory.
- Removed the duplicated year/season status from the bottom command bar and reduced it to a compact single-row dock.
- Added fixed map controls for zoom in, zoom out, and Terrain/Control render-mode switching.
- Added control render mode that colors districts by owner while preserving borders, roads, settlements, and army markers.
- Changed Build flow: selecting an owned land and pressing Build now opens a modal listing Farm/Mine/Market structures, costs, eligibility, and blocked reasons; building from the modal updates resources and district output.
- Verification: `npm run build` passes; targeted Playwright checks confirmed zoom changes camera scale, render mode toggles to control, build modal opens for the selected capital, and modal Farm build spends resources and increases output.
- Screenshot artifacts: `output/web-game/ui-controls-before.png`, `output/web-game/control-mode.png`, and `output/web-game/build-modal.png`.
- Made map scale configurable through `src/game/gameplayConfig.ts`; current config is 30x52 hexes with a target of 40 neutral districts, producing 42 total districts including the two capitals.
- Added fog of war: only owned lands and border lands are visible, explored state persists, hidden districts render under fog, control mode does not reveal hidden ownership, and hidden enemy armies are not drawn.
- Moved zoom/view controls to the bottom-right as compact icon buttons. The view-mode button is now icon-only and switches between terrain and control render modes.
- Changed peaceful land buying from instant ownership to timed acquisition orders. Buying adjacent neutral border land spends gold, leaves the land neutral while progress advances, shows `progress/required` in the land panel, and renders an on-map acquisition marker inside the visible land area.
- Hardened Build modal focus by storing the build target land before opening the modal, fixing the case where pressing Build lost the selected land.
- Verification: `npm run build` passes; targeted Playwright checks confirmed 30x52 config, 42 districts, fog hiding the rival army, build modal preserving selected capital, timed acquisition order creation/completion, and visible acquisition progress marker.
- Screenshot artifacts: `output/web-game/fog-initial.png`, `output/web-game/control-bottom-right.png`, `output/web-game/build-focus-fixed.png`, and `output/web-game/acquire-progress-visible.png`.
- Reworked fog rendering so hidden land is covered by a continuous pale cloud/white layer above terrain and district borders, preventing visible hex tile outlines in fog.
- Verification: `npm run build` passes; Playwright screenshot `output/web-game/shot-0.png` confirms hidden areas no longer expose hex borders.
- Optimized fog rendering by replacing per-hidden-hex cloud primitives with cached district boundary fills, keeping the white fog look while reducing draw shape count.
- Verification: `npm run build` passes; Playwright screenshot `output/web-game/shot-0.png` confirms the optimized flat fog still hides unrevealed tiles cleanly.
- Restored a cloud-like fog look without per-hex rendering by adding three large deterministic puffs per hidden district over the cached district fill.
- Fixed Build focus issues: map input is now blocked over the bottom sheet, the opening camera centers on the player capital, and the main Build command falls back to the visible owned capital when no district is selected.
- Verification: `npm run build` passes; Playwright confirmed main Build opens the build modal, land-panel Build opens the same modal without losing `selectedLandId`, and a modal Farm build spends resources and adds the building.
- Screenshot artifacts: `output/web-game/build-main-modal.png` and `output/web-game/build-panel-modal.png`.
- Implemented court/map/AI/army revamp: randomized campaign seed and district names/types every new game, farthest-apart player/rival starts, mandatory reusable court-card deck with 32 cards / 64 non-stability outcomes, court modifiers, hidden court stat bars, AI timed acquisition orders, player army disbanding, army gold upkeep, and barracks-backed army veterancy.
- Verification: `npm run build` passes; Vite still reports the existing Phaser bundle-size warning.
- Verification: Playwright client generated fresh `output/web-game/shot-0.png` through `shot-4.png`; targeted browser checks confirmed random seeds change on reload, mandatory court request cannot be closed before choosing, court choice resumes play and applies modifiers, rival AI queues acquisition orders before ownership changes, and disband returns surviving soldiers to humans.
- Screenshot artifact: `output/web-game/court-request-mandatory.png`.
- Fixed follow-up UI overflow: compact land bottom sheet now shows garrison/status instead of long blocked-build reasons, and Army disband card explicitly says humans return while XP is lost.
- Verification: `npm run build` passes; targeted Playwright screenshots checked `output/web-game/land-panel-no-overflow.png` and `output/web-game/army-disband-xp-copy-fixed.png` with no console errors.
- Refined map edges and building management: removed the extra world padding, added low-opacity random background hex tiles around the generated map, compacted land-panel text further, and added Build-modal destroy actions for existing buildings to reduce maintenance at the cost of losing that building's output.
- Verification: `npm run build` passes; targeted Playwright screenshots checked `output/web-game/map-edge-faint-tiles.png`, `output/web-game/land-panel-compact-status.png`, and `output/web-game/build-modal-destroy-building-final.png`; targeted browser state confirmed destroy removes one building and refreshes resource rates with no console errors.
- Corrected the map-edge follow-up: restored iOS/world padding and changed the padded area from random/washed filler blocks to normal terrain-style tiles cloned from the nearest real map edge, including normal terrain decorations.
- Verification: `npm run build` passes; Vite still reports the existing Phaser bundle-size warning.
- Added more breathing room around the map by increasing the world padding to 220px; padding tiles still clone the nearest edge terrain and hidden-source padding remains clouded until the source land is visible.
- Verification: `npm run build` passes; Vite still reports the existing Phaser bundle-size warning.
- Added a visible sandy coast buffer for discovered non-water tiles that border water or empty map space, so revealed land no longer cuts directly into dark sea.
- Verification: `npm run build` passes; Vite still reports the existing Phaser bundle-size warning.
- Widened the sandy coast buffer strokes so coastline reads as a beach band instead of a thin outline.
- Verification: `npm run build` passes; Vite still reports the existing Phaser bundle-size warning.
- Fixed coast placement so cloned padding land tiles count as land neighbors; sand no longer draws between visible land and land-like padding, only against water or true empty edge.
- Verification: `npm run build` passes; Vite still reports the existing Phaser bundle-size warning.
- Implemented menu/save/input/performance pass: added a Phaser main menu before gameplay, single-slot localStorage snapshot save/load, gameplay menu with save and prompted exit-to-menu, click-select/click-land army ordering, and fog-aware static map repainting so hidden terrain/roads/labels are skipped under fog.
- Verification: `npm run build` passes; targeted Playwright checks passed for initial menu, start game, save snapshot, cancel exit prompt, save-and-exit, continue from snapshot, exit without saving, start-new confirmation, army select, map drag while army selected, click-to-move order creation, invalid hidden-land tap retaining army selection, and fog screenshot with no hidden labels/settlements. Screenshot artifacts are under `output/web-game/menu-save-flow-3`, `output/web-game/army-click-move-3`, and `output/web-game/army-invalid-hidden`.
- Researched RTS menu patterns and replaced the plain menu with a more cinematic war-table scene: bronze seal title, battlefield map, citadel, army columns, banners, fog bands, and large command-plate buttons. Verification: `npm run build` passes; Playwright confirmed menu -> start -> save/exit -> continue still works with no console errors. Screenshot artifact: `output/web-game/menu-research-final/shot-0.png`.
- Unified menu and in-game visual language: upgraded `InkUI.button` to the command-plate style from the menu so action bars/modals inherit it, routed legacy `createWoodButton` through the same renderer, and redrew `MenuScene` with `InkMapRenderer`/`inkTheme` ink-wash land, roads, fog, mountains, citadel, and troops. Verification: `npm run build` passes; Playwright confirmed menu -> start -> save/exit -> continue with no console errors. Screenshot artifacts: `output/web-game/ink-menu-shared-buttons/shot-0.png`, `gameplay-actionbar.png.png`, and `game-menu-buttons.png.png`.
- Added seeded multi-style player flags inspired by the attached dynasty/banner references: yellow seal, red moon, layered square, red-fringe yellow, and yellow/red medallion variants with pseudo brush glyphs. The waving cloth tween is preserved, and the campaign map seed chooses one stable random style per game. Verification: `npm run build` passes; Playwright captured all five seeded variants with no console errors under `output/web-game/flag-variants-2/`.
- Polished the menu/buttons/flags follow-up: compact in-game buttons no longer draw side notches, press feedback now scales around the visual center, the menu uses in-game cloud rendering plus seeded in-game flag standards, the menu seal no longer overlaps the title, flag glyphs are smaller/centered, and the previous black fringe/fire-like flag cutouts were replaced with clean colored borders. Verification: `npm run build` passes; web-game client confirmed menu mode, targeted Playwright screenshots checked menu, centered button press, gameplay controls, gameplay menu, and all five flag styles under `output/web-game/menu-flag-polish-client`, `output/web-game/menu-flag-polish-targeted`, and `output/web-game/menu-flag-polish-menu-modal/`. Only expected Vite Phaser bundle-size and screenshot ReadPixels warnings were observed.
- Reused `MapRenderer` terrain decorators in the main menu scene for forest/tree clusters, mountain ridges, rice/water strokes, and river texture, replacing menu-only mountain drawing. Moved the in-game Menu control into the resource header as a borderless text hit target and widened the message text area below it. Verification: `npm run build` passes; web-game client confirmed menu mode; targeted Playwright screenshots checked the menu terrain treatment, borderless header Menu label, and opening the game menu from that header label under `output/web-game/menu-maprenderer-header-client` and `output/web-game/menu-maprenderer-header-targeted`. Only expected Vite Phaser bundle-size and screenshot ReadPixels warnings were observed.
- Started settlement art follow-up from screenshot feedback: removed generated red roof finials, disabled ordinary multi-tier/loft settlement roofs, shifted building palettes to muted earth/ink colors, added low market-stall rendering, and routed market districts through a small-shop cluster layout.
- Continued the settlement/farm art revamp from Emperor-style references: removed the remaining blue-tinted shop roof color, changed roofs to brown/grey clay and thatch tones, added timber/plaster details to single-floor houses, enlarged readable house compounds, and replaced flat farm rectangles with isometric field plots plus small farm huts.
- Replaced the generic radial menu medallion, then abandoned the too-complex dragon attempt for a simpler Dai Viet lotus seal: red imperial medallion, gold ring, square seal frame, and readable lotus blossom motif.
- Moved the menu snapshot/save status from under the title to directly below the Continue button so it reads as Continue-specific state.
- Differentiated main menu actions: Start Campaign remains the large primary command, while Continue is now a smaller ghost-style saved-game action with the snapshot label underneath.
- Improved map land-name readability by replacing bare settlement labels with small parchment-backed label containers, including a two-line capital label, so names do not disappear into buildings, mountains, or terrain.
- Fixed EN/VN localization follow-up: split the monolithic i18n catalog into core/world/heroes/politics catalog files, removed stale shrine/partial fallback translation paths, added complete hero display-name/description/effect helpers, added complete Vietnamese politics card/choice text, and routed hero names through UI/system/render text helpers while preserving generated king names.
- Fixed Vietnamese text rendering by moving Phaser UI text onto a Vietnamese-safe font stack across shared UI helpers and direct text paths.
- Verification: `yarn build` passes; web-game Playwright smoke and targeted localization checks pass on localhost:5173 with no console errors. Targeted run confirmed EN default, VN persistence, VN hero draft/politics text, EN hero draft/politics text after switching back, and readable screenshots at `output/web-game/i18n-fix-targeted-font/`.
- Fixed minimap and land-selection input routing: UIScene now publishes minimap hit bounds, MapScene treats those bounds as fixed UI before land picking, minimap taps suppress map input through pointer release, and the compact selected-land card has a small close target that clears selection through MapScene.
- Revamped "Ngai Vàng Các Đế Quốc" (Throne of Empires / `empire` mode) gameplay around four pillars — challenge, reward, progression, and always-having-work:
  - Root fix: invasions were all pre-scheduled at game start (fixed count, last one ~turn 60), so the endless mode stopped threatening the player. Replaced with a continuous `ThreatDirector` (`src/systems/empire/ThreatDirector.ts`) that regenerates a pressure budget scaled by era/difficulty/player-strength, telegraphs waves via ultimatums (minor + once-per-era named-warlord Great Invasions), and can be defused by diplomacy. `CampaignEventSystem` dynasty-attack now only fires for classic campaign.
  - Directives board (`src/data/directives.ts`, `src/systems/empire/DirectiveSystem.ts`): three always-live tiered objectives (Immediate/Standing/Grand) measured against live metrics; completing them pays Mandate + resources and refills. Timed "prep" directives are auto-issued alongside ultimatums.
  - Mandate of Heaven progression (`src/systems/empire/MandateSystem.ts`): a points track filled by directives + battles that advances four eras (Founding→Rivalry→Empires→Heavenly Mandate), each granting edict points, raising the building level cap, and unlocking extra court seats.
  - Edicts & Wonders (`src/data/edicts.ts`, `src/systems/empire/EdictSystem.ts`): a 3-branch tree of permanent upgrades — edicts bought with edict points, resource-funded Wonders — all applied as permanent `CourtModifier`s through the existing `addCourtModifier`/`computeCourtBonuses` pipeline.
  - Reward + end-goal: repelling hosts drops spoils (gold, freed prisoners, Mandate; ×3 for Great Invasions). Endless-but-with-closure "Ascension" prestige win — reaching the Heavenly Mandate era issues a one-time Ascension directive whose completion triggers victory. Cross-run Legacy points + a named rank ladder (Village Chief→…→Emperor of All Under Heaven) persist in localStorage (`src/state/legacy.ts`) and show on the menu / score / victory screens.
  - UI: new "Agenda" action (empire-only 7-button bar) opening the Directives/Mandate board and Edicts & Wonders screens; a top-centre telegraph banner counting down invasions; and a transient toast feed above the action bar. All logic is `gameMode === 'empire'`-gated so campaign/rival modes are unaffected.
  - Verification: `yarn build` passes; targeted Playwright harness checks (`test_scripts/verify/verify-empire-revamp.mjs`, `verify-edicts-ascension.mjs`, `shot-edicts.mjs`, `verify-directives-ui.mjs`) confirm continuous invasions past turn 60, telegraphed minor + Great Invasions, directive turnover, era advancement + seat unlocks, spoils toasts, edict enactment applying a permanent modifier, and the full Ascension→victory path, with no console errors. Screenshots: `output/web-game/empire-directives-board.png`, `empire-edicts-board.png`, `empire-revamp-verify.png`.
- Gameplay-feel follow-ups on Throne of Empires (critique-driven):
  - Combat honesty: player-initiated battles ([WarSystem.attackLand]) are now probabilistic — the shown win % is the real chance (clamped 10-90 so upsets/heroic stands stay possible), and casualties scale with how close the fight was. Invasion defense got a ±10% fuzz so close fights swing without punishing solid preparation. Verified empirically (`test_scripts/verify/verify-combat.mjs`: shown 61% vs actual ~65%).
  - Directives are now all ACTION goals (expand/recruit/build/seat/defend/enact) — removed the passive "amass gold/food/population" timers so the board is a to-do list, not a wait-list.
  - Progression cadence retuned (era thresholds 32/92/190, higher battle Mandate) so a full session reaches the endgame; persistent **Mandate/Era HUD bar** ([UIScene.renderMandateBar]) pinned under the message strip shows era + fill-to-next + points at all times (the "XP bar" that makes progression *felt*), tappable to open the Agenda. Screenshot: `output/web-game/mandate-bar-hud.png`.
  - Header dynasty-stability readout: replaced the old three unlabeled colour strips with a single borderless colour-coded `● Stability NN%` in the header's free top-centre (matches resource-rate palette).
- Living empires (Great Powers sim) — the off-map empires are now dynamic actors, not static pressure:
  - New `src/systems/empire/GreatPowersSystem.ts`: each Kingdom gains an evolving `power` + `stability` (types.ts). Once per in-game YEAR (hooked in RealtimeSystem on the year rollover) empires grow (stable/ambitious realms arm faster), drift in stability, wage inter-empire wars where a victor **absorbs** the loser into a hegemon, and **collapse → are reborn** as a brand-new named realm (new king/personality/relations). `getEmpirePower` now reads the evolving `power` so a realm that grows strong is genuinely more dangerous.
  - ThreatDirector aggressor selection + invasion size are now **strength/ambition/war-appetite/fear-driven** (answers "why doesn't a strong stable empire attack me" — now it does, and bigger).
  - Player statecraft levers (`src/systems/empire/EspionageSystem.ts`, surfaced in the Foreign Affairs detail panel): **Post/Recall Envoy** (standing relations + reveals their hidden Stability), **Foment Unrest** (spend influence to crash a rising threat's stability), **Incite War** (turn a rival empire against another so they bleed each other). Empire detail now shows Power (always) · Stability (?? until an envoy is posted) · king name + temperament.
  - Year-change cinematic: a brief centred "Year N" flourish ([UIScene.playYearTransition]) when the survival clock advances.
  - Verification: `yarn build` passes; `test_scripts/verify/verify-living-empires.mjs` confirms power diverges dramatically over 50 sim-years (48/63/53/54 → 31/157/140/140, 8 rebirths), sabotage crashes stability, ambassador/incite work; `verify-combat.mjs` confirms honest odds; console smoke clean. Screenshots: `output/web-game/empire-detail-espionage.png`, `mandate-bar-hud.png`, `dynasty-stability-header.png`.
- Full-playthrough balance pass (`test_scripts/playtest/playthrough.mjs` — a bot that expands/builds/recruits/defends/court/edicts/espionage, plays to a finish and prints a year-by-year journal + verdict):
  - First playthrough exposed two real defects: (1) a single empire snowballed into an invincible hegemon (P73→P160, conquered everyone — the absorption loop had no brake), and (2) court cards were spammy (~77 in 28 years). (The bot's own 1-land death-spiral was a bot bug — expansion works fine, confirmed by `probe-expand.mjs`; the bot had gated expansion behind gold>90 and starved it.)
  - Fixes in `GreatPowersSystem.ts`: **overextension brake** — power past 88 drags stability down (bloated empires fracture), hard `POWER_CAP=122`, gentler yearly growth, smaller conquest absorption (spoils ×0.5, remainder ×0.5) + more war exhaustion, inter-empire war frequency 0.55→0.4. Hegemons now rise AND collapse instead of snowballing. Court-card base cadence eased 5→7 ticks (`PoliticsSystem`).
  - Result: a competent player now reliably ASCENDS in ~15–22 years across seeds, reaching all four eras, expanding 1→22-35 lands, repelling 7–12 invasions, with hegemons visibly forming and shattering and no console errors. (Normal difficulty is "winnable with good play"; hard/ironman remain for challenge.)
- Instrumented playthrough (`test_scripts/playtest/playthrough-analyze.mjs`) exposed the next-tier problem: the game was winnable but *tension-free* — `minStability 60` (never near collapse), `defenderPower = defense×35` meant a walled capital (~2100) dwarfed any field army (~900), so you could **turtle behind walls to victory** and the standing army was optional; ~23% of ticks were idle.
  - Phase 1 fix "the army matters" (WarSystem `defenderPower` walls 35→16 + localSoldiers×2.5; InvasionSystem siege multiplier so big/Great hosts negate much of the wall bonus). Now a field army is essential, districts actually fall to invaders, and outcomes are uncertain: a mediocre bot wins ~60% (was 100%), minStability dips to 45–60, and defeats are real conquest losses. Combat has weight and stakes for the first time. Remaining depth (tactics/composition/scouting) is planned as later phases.
- Depth pass (Phases 2–6 of the complex plan), all `gameMode === 'empire'`-gated, `yarn build` green, no console errors:
  - **Phase 2 (combat tactics):** unit counters (RPS: spears>heavy>archers>spears) + defensive terrain multiplier in `WarSystem` battle resolution; **recruitment composition doctrines** (Balanced/Spear Wall/Archer Host/Shock — `queueRecruitment` + Army-modal selector, threaded via `ui:create-army`); **battle stances** (Assault/Measured/Cautious trading odds vs casualties — `attackLand` + `BattlePreviewPanel`, which now also **scouts** the enemy host's composition).
  - **Phase 3 (tension):** `CrisisSystem.tickCrises` — province loyalty drifts with dynasty stability, distance from the throne, and imperial overreach; a bitterly disloyal province **secedes** (a real loss short of defeat).
  - **Phase 4 (decisions/brink):** `AbilitySystem` — three cooldown royal commands (**Rally** morale, **Levy** emergency militia, **Decree** stabiliser) surfaced as a Royal Commands row on the Agenda board.
  - **Phase 5 (living world demands engagement):** ThreatDirector now stages a **Grand Coalition** against a dominant player (success breeds threat) and a hegemon issues a **vassalage ultimatum** ("Demand of Submission" — submit for gold/prestige or defy into war), reusing the foreign-card modal.
  - **Phase 6 (identity):** army **elite tiers** (levy→trained→royal guard, from barracks+era) and **generals that grow** — a commander gains martial + traits (Veteran/Conqueror) with battle wins, and a general's martial now lifts the whole host's power.
  - Verification: `test_scripts/playtest/playthrough-analyze.mjs` (full game exercising abilities + foreign-card resolution) confirms coalitions form, elite armies muster, a general reached 18 wins, outcomes vary (ascension/defeat), no errors; `verify-crisis-vassal.mjs` force-confirms secession + vassalage fire. Screenshot: `output/web-game/agenda-royal-commands.png`.
- Planned the next gameplay clarity/replayability rework in `docs/08-three-lane-gameplay-plan.md`: reorganize the current systems into three player-facing lanes, Conquer / Manage / War, restore old manual acquisition choices as first-class Conquer actions, group buildings/laws/hero assignments/random tasks under Manage, group armies/heroes/enemy threats under War, and keep Dragon Ascent's fast cadence/replay rewards without letting autopilot own the default gameplay loop.
- Added the Dragon Ascent-specific detailed plan in `docs/09-dragon-ascent-detailed-gameplay-plan.md`: defines the mode promise, three lane structure, prompt priorities, opening flow, Conquer Council with old acquisition methods, Manage doctrine/project/law plan, War doctrine/wave response plan, lane-based Power Draft, hero summon assignment, enemy variety, map/HUD changes, balance targets, phased implementation, and verification strategy.
- **Dragon Ascent core-systems restore.** The doctrine layer sketched in docs 08/09 (`Manage Council` / `War Council` picking abstract "realm doctrines") was cut: it was an invented abstraction sitting on top of nothing (`AscentManagePlan.activeLaws` was a dead string array), it hid the real systems, and it had never compiled (`ConquestUIScene` referenced five screens that were never written). Those docs describe the **superseded** design; the shipped design is below.
  - **Nothing new was designed.** `createAscentGameState` clones the empire state, so every run already carried `state.court` (8 seats, favor, stability), `state.mandate` (4 eras, edict points), `state.politicsDeck` (the 636-line parliament deck) and `state.kingdoms` (rival empires with power/relations/personality). `advanceAscentTick` simply never called them. This pass wires them to the prompt queue.
  - **Conquer** — `LaneSystem` → `src/systems/ascent/ConquestSystem.ts` (`MarchOrderSystem` folded in and deleted). A province card leads to a method sheet offering **every** acquisition path that province admits — bribe / envoy / intimidate / settle / march in / storm — each priced from the existing `AcquisitionSystem` helpers, unaffordable ones greyed with a concrete reason rather than hidden. Tapping a province on the map opens the same sheet (`ui:ascent-conquer`). The autopilot may now only walk onto empty wilderness; villages and rival provinces need a player order.
  - **Court** — `progressCourt` restored to the tick, so Favor draws heroes over time, governors raise province loyalty, ungoverned provinces drag on stability, and tax fatigue compounds. Gacha and Favor draft merged into one `hero-choice` card; resolving it raises a `court-appointment` card offering the three best postings, each printing the real bonus that hero's stats produce (`formatCourtPositionEffect`). "Await a command" is always offered and outranks every ministry when the realm has no free hero left to raise a host.
  - **Laws** — a `law-choice` card spends Mandate edict points on `REALM_PROJECTS`, naming the sibling each exclusive-group edict permanently locks out. Carries the tax dial on the same card.
  - **Parliament** — the politics deck drawn without replacement (`ascent.drawnCourtCards`), same crisis/season/hero-bias weighting as the classic mode, resolved through `choosePoliticsCard`. Always offers "decide nothing" so an unaffordable pair can never corner the player.
  - **World** — `tickGreatPowersYear` + `tickDiplomacy` now run in ascent (guards widened to `isEndlessMode` / `+ 'ascent'`, additive), so rivals arm, destabilise, war on each other and are reborn. An `envoy` card offers gift / trade / tribute / post-ambassador, feeding back into `WaveDirector.pickAggressor`, which weights by `100 − relations`.
  - **Pacing** — `src/systems/ascent/DecisionDirector.ts` owns *when* the game interrupts: one prompt per tick, ≥2 ticks of play between scheduled cards (stamped in `drainAscentPrompts`, the single choke point), per-kind cooldowns, readiness gates, and a starvation guard that forces the best ready card after 4 quiet ticks. Wave and terminal prompts deliberately bypass the gap.
  - **The classic bottom menu** (follow-up): the mode was all-cards with three tiny floating chrome buttons in a corner. It now uses the shared `ActionBar` with the menu the rest of the game already defines — `ASCENT_KEYS` = **Build · Heroes · Court · Army · Affairs · Codex · Pause** (empire's 7-key metrics) — and each button opens a real screen built on this scene's card components against the same systems: build/upgrade a district (`getBuildOptions` / `buildDistrictBuilding` / `getUpgradeOptions` / `upgradeDistrictBuilding`), the hero roster with every posting resolved, the court's seats + laws in force + unspent edict points, standing hosts with a "Raise a host" levy (`raiseHostNow`, extracted from `autoRecruit` so the player can overrule the target-host-count gate), and the rival empires. Conquest deliberately has **no** bar button: like the classic modes it is reached by selecting a province on the map, which already opens the full method sheet.
  - `ActionBar` gained one optional `statusColor(action)` hook — undefined for every other mode, so their bars are byte-for-byte unchanged (verified against an empire screenshot). Dragon Ascent lights a dot only where the condition is about that screen: an unposted hero on Heroes, low stability or unspent edict points on Court, threat outrunning defence on Army, a hostile neighbour on Affairs, an idle treasury on Build.
  - The top lane strip and the corner chrome are gone; the inspect card sits clear of the bar, and `isScreenPointOverFixedUi` treats the bar band as fixed UI so tapping a button no longer drags the map underneath.
  - Two defects the new screens exposed and fixed: `refresh()` decided bar/inspect visibility from *last* tick's prompt key, leaving the bar hidden for a frame after a card chain ended (now reconciled first); and the shared `reason.needEra` string read "Unlocks in the Age of Age of Rivalry" because the template repeated what `eraLabel` already returns — a pre-existing copy bug in the classic build screen too.
  - **Map legibility pass** (follow-up), three fixes:
    - **Map controls restored.** Zoom in / zoom out / terrain-control toggle were lost when this mode stopped using `UIScene` — `MapScene` already listened for `ui:zoom-map` and `ui:toggle-render-mode`, but nothing emitted them. `ConquestUIScene` now draws the same right-edge icon stack the classic modes use, and the stack shifts above the province inspect card (`inspectCardTop`, shared by both) so the two never overlap.
    - **HUD band cut from 106px to 60px.** POWER, THREAT and MOMENTUM now sit on two dense rows instead of three airy ones, with the ahead/even/behind verdict inline beside the threat figure rather than under it. With a 44px header and a 50px action bar, the band was eating a fifth of the screen; the map is the game. `ASCENT_HUD_BOTTOM` moved 150 → 104 to match.
    - **Ownership readable in terrain view.** Terrain mode showed ownership only as a thin outline, so on an all-neutral opening map "which of this is mine" was unanswerable. `ConquestScene.repaintOwnershipTint` now washes every district the realm does *not* hold — neutral in a cool dark, rivals in their own colour — leaving our ground the only bright area. Tinting our own land instead does not work: the player colour is jade and the map is green grass. Painted live at depth 1.95 (between the static bake at 1.9 and land markers at 2 — below the bake it is simply covered) and only when the ownership map actually changes; ascent-only, so classic modes are untouched.
  - Verification: `npx tsc --noEmit` clean; `test_scripts/verify/verify-ascent.mjs` (rewritten, 36 checks) PASSes — ≥3 acquisition methods used, ≥2 seats filled, ≥2 edicts with no exclusive-group collision, deck never repeats, rivals evolve, scheduled cards never land on consecutive ticks, no prompt left unanswerable. `verify-modes-regression.mjs` output is **byte-identical to HEAD** for empire/campaign/rival. New `test_scripts/shot/shot-ascent-core.mjs` captures all nine screens. (`verify-empire-revamp.mjs` reports unmet expectations both before and after this change — pre-existing, untouched.)
- **Dragon Ascent challenge pass.** A real playthrough reached Year 9 (turn ~34, two minutes in) with gold 2.1k at +243/tick, one or two waves faced, and no rival having ever acted — "very less challenge, economy grows too fast, no actions from other empires". Four separate causes, all fixed:
  - **Waves were sized against the wrong number.** `launchOffMapInvasion` clamped invader size to `getPlayerMilitary` — a raw headcount + `defense×10` — while the realm's real defence runs through `armyPower`, which multiplies by court `armyPowerMult`, level, elite tier and general martial. Every Power Draft card raised the player's defence and nothing on the invader side. Added `InvasionSpawnOptions.totalSoldiers`, which supplies an exact budget and bypasses the clamp; empire mode never passes it, so its behaviour is untouched (regression byte-identical).
  - **Waves now track the realm's *lagged field* power.** `WaveDirector.laggedDefencePower` reads `computeFieldDefencePower` (hosts + capital garrison) from `WAVE_LAG` waves ago. The lag is the design: outpacing the curve buys two genuinely easier waves, coasting lets it close — which is what makes a pick feel like it changed the outcome. New `computeFieldDefencePower` exists because summing *every* province's walls badly overstates what meets a wave at one place; sizing against the total made the first tuning pass lethal.
  - **Two structural bugs found by measurement, not by reading.** Waves accumulated — a 12-tick cadence spawns the next before a conquest host has finished marching, and four hosts were live at once by wave 5; the pressure curve is now a target for *total live threat*, deducting what is already marching and skipping the spawn entirely when the map is full. And raids reused the wave budget and its `MIN_WAVE_SOLDIERS` floor, so an early raid was as big as the wave it preceded and two stacked on the capital ended runs by turn 39; raids now have their own share, floor, and a `RAID_WAVE_CLEARANCE` window so they never overlap a wave.
  - **Rivals act on their own** — new `src/systems/ascent/RivalDirector.ts` raising tribute demands, coalitions and vassalage ultimatums through the prompt queue, plus border raids from the wave director. Deliberately not done by widening `tickThreatDirector`'s gate: it writes `pendingUltimatum`/`pendingForeignCard` and sets `isPaused` itself, which this mode's queue does not read and its UI does not render — so it would have deadlocked the run. Its helpers and dominance shape are reused. Every refusal lands on the wave curve (`ticksToWave` pulled forward, war appetite raised, an endured coalition returns as a heavier multi-host wave) rather than in a toast.
  - **Gold got somewhere to go.** A `hire-mercenaries` response option, income-pegged like `fortify`/`buyOff`, spawns a company immediately with no commander and no manpower — the treasury-to-survival converter. With that plus tribute demands, a measured run ended on ~1.5k gold instead of 14k.
  - Tuning lives entirely in `ascentConfig.ts`. `WAVE_PRESSURE_BASE` is the main dial and is well-conditioned: measured against a naive auto-player, 0.45 → survives ~83 seasons, 0.55 → ~73, 0.62 → ~65. Shipped at **0.5**; a thinking player fortifies, keeps a host home and buys off coalitions, so the real ceiling is well above that.
  - **A dead branch caught before shipping.** The vassalage gate asked for a rival at 1.8x `getPlayerMilitary`, but an off-map empire's strength is its `power` index (capped 122) scaled x10, so the strongest rival measures **0.45-0.70x** the player's military across an entire run — the branch could never fire, and tribute fired only in the opening seconds. Both ratios are now calibrated from measurement (`VASSAL_POWER_RATIO` 0.66, `TRIBUTE_POWER_RATIO` 0.45) and a long run produces all three kinds (coalition 12, tribute 6, vassalage 2). `verify-ascent` asserts each kind is reachable rather than just counting the total, so the branch cannot go dead again.
  - Verification: `verify-ascent.mjs` now 43 checks including the challenge contract — waves track the realm rather than a fixed curve, raids happen, rivals demand unprompted, the realm actually loses ground, and gold does not end unspent. Because a normal-difficulty run is now short *by design*, the slow-burn systems (era progression, edicts, envoy) are exercised by a second 500-tick run on easy: it reaches the Mandate era with 11 edicts and 42 provinces. `verify-modes-regression.mjs` byte-identical to HEAD; `verify-living-empires`, `verify-edicts-ascension`, `check-console` all pass.
  - Test scripts now target `localhost` rather than `127.0.0.1` — the dev server binds IPv6-only on this machine, which made every driver script fail to connect.
- **Đông Hồ UI/UX defect pass** — four reported faults on the default theme, each traced to a cause rather than repainted:
  - **A host was standing in the river.** `drawDongHoArmies` held four literal x/y pairs while the river's course lived in `drawDongHoLandscape`, so when the course last moved nothing could notice the far-left host had ended up inside the water. The course is now `createMenuRiver()`, which answers `spanAt(y)` off the *drawn* bank polygon (the band is offset perpendicular to a diagonal course, so its horizontal extent at a given height is much wider than its width — sizing against the width is how the old placement looked safe), and `planDongHoHosts` derives every position from it. The field lattice reads the same host footprints, so paddy is drawn around the men instead of under them.
  - **The fields.** Sixteen independently-placed rectangles, each with its own outline, its own random size and its own gaps — paper scraps dropped on the grass. Replaced with the paddy lattice the map's delta already uses, lifted out of `DongHoMapRenderer.paintFields` into `ink/settlements.paddyLattice` and shared: bund lines run the width of the field system and every plot is cut from the strip between two of them, so neighbours share their banks. Kept to three field systems around a far-bank hamlet, with trees and farmers breaking them up, because a lattice will happily fill a bank edge to edge and turn it into a course of masonry (it did, twice, on the way).
  - **"Game icons look scale not correct: width larger than height."** Real, and worse than it looked: the whole art layer is scaled `(1, vScale)` to fit short phone sheets, and the lotus seal was being collected into it — on a 664-tall sheet a 96-unit circle came out 96 wide and 66 tall. The seal is now drawn outside that layer and scaled *uniformly*; the hosts and their standards moved out with it, since a squashed banner is the wrong shape at any factor but one. The emblem itself was also five wide `fillEllipse` calls making a fan 66 across and 44 high, spilling past the cartouche meant to frame it; rebuilt from petals swung out around the stem, 35 wide inside a 40×45 frame.
  - **The army render.** The menu measured each block with `hostShape(men, 4.2, 3.6)` and drew it with `drawHost`'s own `hostShape(men, 4.6×0.74, 4×0.74)` — a fifth smaller in both axes — then put a round `groundShadow` at `y + 3`. Net effect: a shadow roughly eight pixels below the feet of a twelve-pixel block and offset right of it, which is exactly what "the shadow place incorrect" is looking at. Both now use the block that is actually drawn, through the shared `hostFootprint` the map's own marker already used. And a host had no cadence at all: `drawHost` gained an optional per-rank target so each rank can take its own object, and `marchInPlace` gives them phase-shifted strides — per rank rather than per figure, three tweens per host instead of forty on a map that carries dozens.
  - **The buffalo were static** because `ink/props.buffalo` draws into whatever shared `Graphics` it is handed, and an animal baked into the same buffer as the roofs behind it can never take a step. New `src/ui/ink/life.ts`: `livingProp` gives one prop its own object, `grazeInSmallArea` walks it around a bounded patch of its own field. Applied to the menu's herd and to every settlement and farm on the map.
  - **The cart drove backwards.** `TrafficRenderer.walk` flips a mover by the sign of its step, which assumes every glyph is drawn facing +x; the xe trâu is drawn facing left and only said so in a comment. So it was wrong on *both* legs — mirrored going left, unmirrored going right, the animal pushing its own cart either way. `life.ts` now owns the convention: a glyph declares `setNativeFacing(-1)` and `faceTravel` multiplies that by the direction of travel.
  - **Two faults the first attempt introduced, caught on review of the screenshots:** the herons added for motion crossed the karst face rather than open sky (this composition has none — the rock reaches the title's own rule) and a filled `heron` at the size that fits reads unmistakably as a fish, so they are gone; and extracting `hostFootprint` moved the ellipse a third of a block to the left, because the map's inline version centred it on the marker origin while the extracted one was handed the block's left edge. The footprint is now computed from where the feet actually land — `shape.width/height` are the block's *pitch* and overshoot the outermost figure by a full spacing in each axis, and `y` is the BACK rank, so sizing off them was always a spacing low — and both callers pass the same anchor as their `drawHost` call. `verify-living-map` now measures it at four host sizes against the recorded figure positions, so it cannot drift a third time.
  - Verification: `npx tsc --noEmit` and `yarn build` clean. New `test_scripts/verify/verify-living-map.mjs` asserts the whole bug report — no host's bounds intersect the river on a tall *or* a short sheet, the seal's scaleX equals its scaleY at both sheet heights, the menu runs 32 live tweens, 292/292 sampled cart steps face their direction of travel with both legs exercised, the herds move but never leave their radius, and the extracted lattice produces output byte-identical to the loop it replaced (compared plot-by-plot in the browser). `verify-modes-regression.mjs` byte-identical to HEAD; `verify-ascent`, `verify-menu-fit`, `verify-render-scale`, `check-console` all pass. Screenshots in `output/dongho-life/`.
- **Header/HUD space pass** (follow-up on the same theme): the Dragon Ascent readout closed itself with a răng cưa frieze drawn immediately under its gold level bar, and then a hairline under that — three horizontal rules in eight pixels, two of which carry no information, and the eye reads a bar of teeth directly beneath a progress bar as more data. The frieze is gone and the eight pixels went upward, where they were needed: the 44px resource strip carries a frieze at each edge plus the year/season line plus four stores, and in Vietnamese the title's own diacritics (the breve on "Năm", the circumflex on "Xuân") reached into the top band while the numbers' parentheses sat on the bottom one. `HEADER_HEIGHT` 44 → 52, `ASCENT_HUD_HEIGHT` 66 → 58, HUD rows tightened to match — so Dragon Ascent's chrome is exactly as tall as it was (110px) and none of its map is lost. Empire and campaign pay the 8px, since they have the same header and nothing below it to reclaim from.
  - Also fixed while in there: `ASCENT_HUD_BOTTOM`, the tap guard that separates HUD from map, was a hand-kept `104` against a band that closed at 110 — the bottom six pixels of the readout passed taps through to the map underneath. It is derived from the two heights now.
  - Verification: new `test_scripts/verify/verify-header-fit.mjs` reads the real rendered text bounds against the exported band geometry (`HEADER_BANDS`) in both languages and both modes — title clears the upper frieze, stores clear the lower one, the readout ends inside its own band, and the chrome is asserted to be no taller than 110. `verify-modes-regression` byte-identical, `verify-ascent`, `verify-render-scale`, `verify-menu-fit`, `verify-living-map`, `check-console` all pass; `yarn build` clean.
- **Props: paint order, the people, and the herd's cost.** Researched the sprite question first, since the answer decides all three: Phaser's own guidance is to bake a `Graphics` whose shape does not change (`generateTexture`) and stamp it as images, and to keep the number of distinct textures small because copies of one texture batch into a single draw call while alternating textures flush the batch per object. The repo already had the authored-asset half of this — hero portraits are SVGs in `public/faces/` rasterised once at load — so the shape of the answer was already in the codebase.
  - **Render order.** A settlement drew in the order the code happened to mention things: hamlets, the seat, then every tree. So a tree standing *in front of* a roof was painted over it, and the herd — added to the container before the buffer with the houses in it — was behind every roof wherever it stood. New `GroundPart` + `paintByGround` sort a settlement's parts by the ground each one stands on and paint back to front, splitting the shared buffer around any live object, because a container renders children in list order regardless of depth. The scatter over open country was already y-sorted; it was only the clusters that were not.
  - **The farmer** was two strokes: one bent line and a triangle. It now has a nón lá with a real brim and rib, a head, a tunic in nâu or indigo (the cloth has to be lighter than the limbs or the whole figure is one dark blob), legs that stand apart, a ground shadow, and three poses chosen by seed — bent planting with a seedling bundle, carrying a đòn gánh with a basket swinging at each end, or standing on a hoe — facing either way, so a paddy holds a scene instead of one silhouette repeated.
  - **The herd is baked.** Each animal was a live `Graphics` of several hundred path segments, and a live `Graphics` re-submits its whole command list every frame — forty-two of them, sixty times a second, to show animals whose shape never changes. New `src/ui/ink/sprites.ts` bakes a prop once at `RENDER_SCALE × 2` and stamps it as images; `bakeProp` hands the draw callback its anchor already in raster pixels rather than scaling the object, because `generateTexture` rasterises through the Canvas API. **Size is an instance scale, not part of the texture key** — keying on size gave fifteen textures for forty-two animals, which trades the per-frame cost for a batch flush per size. Four drawings plus a rider flag now cover the whole herd: measured at 42 images over 7 textures, 0 live graphics.
  - `BASE_SCALE_KEY` exists because a baked prop is drawn large and scaled down, so anything that flips it has to multiply by that scale rather than overwrite it — `setScale(-1, 1)` on one of these shows it four times the size, mirrored.
  - Verification: `verify-living-map.mjs` gained three checks — the herd is images not graphics, the herd shares few enough textures to batch, and settlements paint back to front around their animals. Also fixed a measurement bug in that file: it revealed the whole map once and then measured twenty seconds later, by which time the empire tick's own fog had put the map back to its six opening districts, so a check that should have covered forty-two settlements was quietly reading five. `verify-modes-regression` byte-identical; ascent, header-fit, menu-fit, render-scale, console all pass; `yarn build` clean. Screenshots: `output/props/`.
  - **Reference pass** against a Vietnamese woodcut-style illustration the user supplied (a *Ngẫm Lại Sử* frame). Not cut up for sprites — it is someone else's watermarked artwork, and a crop is also fixed at one size and one lighting where a procedural prop scales with zoom, varies by seed and tints per theme. Drawn *against* instead, which is the same method the buffalo already came from. Three things the reference teaches and the code now does:
    - **A canopy has a light side and a dark side.** `tree` was one scalloped ring — a flat green stamp however nicely its edge wobbled, and a hillside of them read as wallpaper. It now carries a shaded crescent along the lower-right of the crown and a lit lobe up-left of centre, so the crown reads as a ball of leaves with the sun on one shoulder.
    - **A delta is a patchwork because its plots are out of step.** `drawFieldPlot` had three states; three over a whole map still reads as a repeating pattern. Five now: under water, turned and bare (plough lines, nothing growing), transplanted rows, gold and ready, and a seedling nursery under a fine lattice — the one field pattern that reads as *worked* rather than merely planted, and the thing the reference prints wherever a village keeps a nursery.
    - **The people are small and upright with a modest hat.** The new farmer's brim was wider than its own shoulders, which read as a mushroom; the hat is down from 3.5 to 2.9 units across and shallower. At map scale the figures now read as people carrying things.
    - Screenshots: `output/props/ref-trees.png`, `ref-fields.png`, `ref-village.png`.
- **Farmer and buffalo silhouette polish from map-scale feedback.** The farmer is now built as one continuous human silhouette with a controlled planting lean, balanced planting/carrying/standing poses, an arm visibly supporting the carrying pole, and low-registration nón lá colour so the print offset cannot read as a second hat. The buffalo's belly was lifted and all four walking legs lengthened, while both horns were shortened into compact crown-rooted crescents. The mounted variant now has a coherent child rider (two straddling legs, tapered tunic, face/hair, joined arms and flute), a smaller parasol, and a rider-specific texture box that no longer clips the lotus canopy. Verification: `npx tsc --noEmit`, `yarn build`, the required web-game client capture, `verify-living-map.mjs`, and `check-console.mjs` all pass; 42 live grazing animals remain baked into 7 shared textures with no console errors. Inspection images: `output/props/lod-farmers.png`, `output/props/lod-buffalo.png`, and `output/web-game/people-buffalo-polish/shot-0.png`.
- **Menu scenery aspect-ratio fix.** The short-sheet menu fit used to scale its entire landscape `(1, vScale)`, so at 664px the banyan, houses, people, and mountains were only 69% of their authored height while retaining full width. The broad ground washes, paddy and river still compress as layout geometry, but 21 recognizable scenery objects are now drawn around individual anchors and counter-scale the parent so their world X/Y scale remains exactly equal; live hosts and buffalo keep their existing fitted positions. `verify-living-map.mjs` now asserts this contract at 844px and 664px. Verification: required web-game client capture visually reviewed at the wide/short 390×620 design, tall and short screenshots reviewed, all 18 theme/language/quality combinations pass `verify-menu-fit.mjs`, `verify-living-map.mjs` passes, `check-console.mjs` is clean, and `npm run build` passes. Final screenshots: `output/web-game/menu-aspect-fix-2/shot-0.png`, `output/dongho-life/menu-844.png`, and `output/dongho-life/menu-664.png`.
- **Lane settings + Chronicle navigation UI/UX pass.** Moved the claim confirmation policy from a Build-list card into the standard fixed footer checkbox; moved the muster confirmation policy out of the system menu and into the Army screen's fixed footer checkbox. Added a reusable fixed tab strip to lane pages and split Sử Ký into Actions, Ongoing, Heard, and Recorded shelves with counts and shelf-specific empty states. `render_game_to_text` now reports the open ascent lane, Chronicle tab, and all three footer policies. Verification: `npm run build`, `verify-lane-settings-tabs.mjs` (17/17, including English/Vietnamese canvas taps, populated Chronicle shelves, and 390×844/390×620 visual bounds), `verify-auto-claim.mjs` (11/11), `verify-muster-proposal.mjs` (7/7), `verify-chronicle.mjs` (15/15), and `verify-modes-regression.mjs` all pass; the required web-game client capture is clean. Screenshots: `output/web-game/lane-settings-tabs/` and `output/web-game/ui-settings-tabs-client-final/`.
- **Menu utilities + flag language picker.** Converted How to Play, History, and Settings into genuinely frameless ghost controls with distinct ink icons while retaining their full hit rectangles. Replaced the text-only front-page language line with procedural Vietnamese and UK flags, always ordered Tiếng Việt then English; the same ordering/flags now appears in the first-run language card, and Settings keeps the same Vietnamese-first order. Menu text-state now exposes utility actions and language-option order. Verification: `npm run build`, `verify-menu-icons-flags.mjs` (10/10 interaction and route checks), `verify-menu-fit.mjs` (all 24 theme/language/viewport combinations), and `verify-guided-run.mjs` (45/45) pass. The required web-game client capture and tall/short/first-run screenshots were visually reviewed with no console errors. Screenshots: `output/web-game/menu-icons-flags/`, `output/web-game/menu-icons-flags-client-final/`, and `test_scripts/shots/tour-1-vi-620.png`.
- **Menu utility-row feedback.** Replaced History's ambiguous hourglass with a dedicated open-book glyph and reduced the utility controls from a 111-unit to a 90-unit centre rhythm, retaining 90-unit invisible hit targets. Frameless icon buttons now reclaim the border padding they do not draw, keeping the English “How to Play” label on one line at 390×620. Verification: `verify-menu-icons-flags.mjs` passes 11/11 including the icon/spacing contract and all three routes; all 24 `verify-menu-fit.mjs` combinations pass; `npm run build` passes; the required web-game client screenshot and text state were visually reviewed with no console errors. Final screenshot: `output/web-game/menu-utility-row-final-2/shot-1.png`.
- **Vietnamese default language.** Changed the no-preference fallback, launch splash copy, accessibility language, and first menu render to Vietnamese. A deliberately saved English choice still overrides the fallback, and live language changes now update the document language. Verification: `verify-menu-icons-flags.mjs` passes 12/12, including an empty-storage assertion for Vietnamese plus the persisted-English path; `verify-splash.mjs` passes all eight language/viewport combinations; `npm run build` passes; the required web-game client reports `language: "vi"` and its compact screenshot was visually reviewed with no console errors. Final screenshot: `output/web-game/default-vietnamese-final/shot-1.png`.
- **Compact secondary menu actions.** Reduced Classic Modes and Continue from the primary action's 282-unit width to centred 240-unit plates, and from up to 46 units tall to 34–38 units. Both retain at least a 44-unit invisible touch height. Verification: `verify-menu-icons-flags.mjs` passes 13/13 including visual and touch bounds; all 24 `verify-menu-fit.mjs` combinations pass; `verify-classic-page.mjs` passes 13/13 after making its English-language fixture explicit; `npm run build` passes; the required compact web-game capture was visually reviewed with Vietnamese text-state and no console errors. Final screenshot: `output/web-game/menu-secondary-compact-final/shot-1.png`.
- **Dragon Ascent attack-target icon.** Replaced the front marker's two thick diagonal strokes—which collapsed into a red cancel/error X at map zoom—with the shared battle clash device: two pale, dark-outlined blades with visible points, guards, grips and pommels over a small cinnabar impact burst. The soft province wash remains, and the blocked/holding state now passes its gold accent into the same device. Verification: new `verify-attack-front-marker.mjs` passes 9/9 for active/blocked semantics and browser errors; `verify-ascent.mjs` passes its full simulation; `npm run build` passes; the required web-game client is clean. Mobile inspection screenshot: `output/web-game/attack-front-marker.png`.
- **Secondary menu hierarchy follow-up.** Reduced the Classic Modes and Continue plates again, from 34–38 units to 30–32 units tall, and reduced their title type from 14/13px to 12/11px (with the Continue note fitting down as the quietest line). Their invisible hit rectangles remain 44 units tall. Verification: `verify-menu-icons-flags.mjs` passes 13/13 including the new visible-height/type-size/touch-height contract; all 24 `verify-menu-fit.mjs` theme/language/viewport combinations pass; `npm run build` passes; the required client screenshot was visually reviewed with Vietnamese text state and no console errors. Final screenshot: `output/web-game/menu-secondary-smaller-client/shot-0.png`.
- **Inline install footer mark.** Removed the oversized one-off download drawing from the lower-left corner, added `install` to the shared `CardIcons` vocabulary, and rendered it at the same 0.62 scale (16.12 visual units) as the utility-row icons. When the front-page build stamp exists, icon + six-unit gap + version text are measured and centred as one group on the same visual baseline; its invisible 44×44 hit area remains fully inside the canvas. Verification: `verify-menu-icons-flags.mjs` passes 14/14 including a synthetic real `beforeinstallprompt` state, exact icon size, baseline, group centre, and hit dimensions; all 24 `verify-menu-fit.mjs` combinations pass; `npm run build` passes; the required client is clean. Final install-available screenshot: `output/web-game/menu-icons-flags/menu-vi-844.png`.
- **Single-line support actions.** Removed the “— or even better,” / “— hay hơn nữa,” connector and its dead catalogue entries, then centred Coffee and Contribute as two independent links on one baseline in both languages. Their visual gap is 18 units, which leaves four units between the padded hit rectangles rather than merging the actions. iOS shell builds still omit Coffee and render the capitalised repository link alone. Verification: `npx tsc --noEmit`; `verify-menu-icons-flags.mjs` 16/16 at 390×844 and 390×620; all 24 `verify-menu-fit.mjs` combinations; `npm run build`; `npm run build:shell`; `verify-shell.mjs` 20/20; required client text-state/screenshot clean. Final screenshots: `output/web-game/menu-icons-flags/menu-vi-844.png`, `output/web-game/menu-icons-flags/menu-vi-620.png`, and `output/web-game/support-links-inline-client/shot-0.png`.
- **Peaceful Vietnamese farmstead menu graphic.** Replaced the default Đông Hồ menu's miniature battlefield—four hosts, military standards, buffalo, scattered people, trees and tiled paddy fragments—with a quiet dynastic river landscape: the existing layered karst mountains, one animated river, six broad adjoining rice plots entirely on its right bank, two aspect-safe nhà ba gian farmhouses, and a large lotus cluster. The houses deliberately use the rural prop language with no temple finial; only the river carries motion, reducing both graphic noise and menu tween count from 32 to 8. Menu buttons, language controls, accessibility state and routes are unchanged. Verification: required web-game client capture and tall/compact screenshots visually reviewed; `npx tsc --noEmit`; `verify-living-map.mjs` ALL PASS (including unchanged map paddy, cart, herd, host and depth contracts); `verify-menu-icons-flags.mjs` 16/16; all 24 `verify-menu-fit.mjs` combinations; and `npm run build`. Final screenshots: `output/web-game/peaceful-menu-second/shot-0.png`, `output/dongho-life/menu-844.png`, and `output/dongho-life/menu-664.png`.
- **Promised-quality menu illustration follow-up.** The procedural farmstead met the noun list but scored only 4.5/10 against the approved artifact: its river was a stripe, paddies were map rectangles, houses were tokens, lotus was too small, and mountains had no atmospheric depth. Replaced the default-theme centrepiece with a dedicated 1536×1024 art-only historical landscape generated from the final approved farmhouse reference using the built-in image tool: layered northern karst, a broad S-curve river, large foreground lotus, organic right-bank terraces, and exactly two rural straw-roof houses. `PreloadScene` loads the project-local `public/art/menu-farmstead-v2.png` before the splash lifts; `MenuScene` preserves its 3:2 aspect, contains it on compact sheets, safely overscans it on tall sheets, and feathers its native paper into the game parchment below the live logo and controls. A second attempted transparency extraction produced a fake checkerboard and was rejected/removed. Verification: required client capture clean; final 390×844 and 390×664 screenshots visually reviewed; `verify-living-map.mjs` ALL PASS (asset dimensions/composition/aspect/fit plus unchanged map behavior); `verify-menu-icons-flags.mjs` 16/16; all 24 `verify-menu-fit.mjs` combinations; `npx tsc --noEmit`; and `npm run build` (8.50 MB service-worker shell including the new art). Final screenshots: `output/web-game/menu-illustration-final/shot-0.png`, `output/dongho-life/menu-844.png`, and `output/dongho-life/menu-664.png`.
- **True layered menu animation.** Rebuilt the approved 1536×1024 farmstead as four registered raster plates: an opaque river/paddy base plus transparent mountains, farmhouse/tree, and lotus layers. `scripts/chroma-to-alpha.mjs` converts generated `#00ff00` isolation plates into feathered true-alpha PNGs with green-spill removal; the three temporary key plates were then removed from `public/art` because their recoverable ImageGen originals remain outside the build. `PreloadScene` loads four distinct textures and `MenuScene` composites them in one responsive art container. The stable river base prevents motion sickness and seams, while distance controls the animation range: slow mountain drift, one-pixel farmstead breathing, and the strongest foreground lotus sway. Existing river glints, valley mist, hearth smoke, and tap-to-ripple feedback remain local effects above the raster plates. `render_game_to_text` now declares the four art layers and six ambient motions. Verification: required web-game client state/capture clean; 390×844 and 390×664 composites visually inspected with no green fringe or UI collision; `verify-living-map.mjs` ALL PASS (four unique textures, authored aspect, independent active tweens, stable ground, tap ripple, and unchanged map behavior); `verify-menu-icons-flags.mjs` 16/16; all 24 `verify-menu-fit.mjs` combinations; `npx tsc --noEmit`; and `npm run build` (12.74 MB service-worker shell). Final screenshots: `output/web-game/layered-menu-client/shot-2.png`, `output/dongho-life/menu-844.png`, and `output/dongho-life/menu-664.png`.
- **Natural river gestures, mountain-depth mist, and historically grounded village placement.** Corrected the upper-right bank after checking northern Vietnamese village structure: the paddies now end at a dry raised verge and footpath, while exactly two houses and their bamboo/orchard screen form one compact settlement outside the planted plots; the misplaced freestanding field tree is gone. Replaced the affected art with `menu-layer-ground-v2.png` and a true-alpha `menu-layer-farm-v2.png`, keeping the original v1 plates recoverable. The river now uses varied spline-following current lanes rather than repeated glints, with distinct tap ripples, drag wakes, and throttled hover wakes. Three softly feathered mist bands animate in a dedicated nested layer between the mountains and settlement, so fog builds depth without washing over the houses or controls. Verification: final required client capture and 390×844/390×664 screenshots visually inspected; `verify-living-map.mjs` ALL PASS (v2 plates, dry-verge metadata, two-house contract, three mist bands, currents and all water gestures, unchanged gameplay map behavior); `verify-menu-icons-flags.mjs` 16/16; all 24 production-preview `verify-menu-fit.mjs` combinations; `npx tsc --noEmit`; and `npm run build` (15.23 MB service-worker shell). Final screenshots: `output/web-game/menu-water-hamlet-final/shot-2.png`, `output/dongho-life/menu-844.png`, and `output/dongho-life/menu-664.png`.
- **Rejected-house regression correction.** The first village-verge revision was rejected after visual review: its bright standalone house plate sat across the mountain plane, its circular fence/mound read as a floating fortified island, and scene-level river marks painted above the lotus despite their numeric depth. Replaced the base with `menu-layer-ground-v3.png`, whose lower-right dry hamlet verge is connected to the paddy dikes with no ring or field tree, and replaced the architecture with a muted northern three-bay main house plus smaller perpendicular service house. Built-in transparent output twice baked a fake checkerboard, so the accepted house drawing was isolated against `#00ff00` and converted through the existing spill-removing `chroma-to-alpha.mjs`; the temporary key plate was removed. The generated full-frame plate ignored requested coordinates, so production now measures its real opaque bounds (`719..1466 × 670..910`), scales them to 52%, and anchors their centre at source-space `1230,600` on the right-bank verge. All current/ripple/wake objects now live in a `river-fx` child between mist and architecture, guaranteeing water is below both houses and lotus; the spline was also re-read against the v3 river. Verification: required client state clean (its WebGL-only screenshot path returned black, so rejected as visual evidence); 390×844 and 390×664 page captures visually inspected; `verify-living-map.mjs` ALL PASS including real corner alpha, house-band/transform, water ordering, gestures and unchanged map behavior; `verify-menu-icons-flags.mjs` 16/16; all 24 `verify-menu-fit.mjs` combinations; `npx tsc --noEmit`; and `npm run build` (17.77 MB service-worker shell). Final screenshots: `output/dongho-life/menu-844.png` and `output/dongho-life/menu-664.png`.
- **Unified house print style, readable atmosphere, and reactive lotus.** Re-rendered only the two-building plate against the mountain and lotus references so the houses now share their pale translucent wash, broken ink weight, and low-detail paper-print character instead of reading as a separate architectural illustration. The accepted `menu-layer-farm-v4.png` is true-alpha, keeps exactly two buildings on the connected right-bank verge, and is positioned from its measured opaque bounds (`690..1474 × 660..946`). Strengthened and accelerated the river currents, increased tap/drag wakes, and added a runtime movement contract proving the strokes move beneath the farm and lotus layers. Four mountain-base mist bands now combine a readable paper veil, blue-grey lower edge, and drifting wispy line. The lotus uses separate ambient and damped pointer-spring proxies, so hover or drag bends the flowers and release eases them naturally home without fighting the breeze. Verification: required web-game client run; final 390×844 and 390×664 captures visually reviewed; `verify-living-map.mjs` ALL PASS (four mist bands at ~0.49 runtime alpha, seven of seven sampled currents moving, pointer reaction, exact farm alpha bounds and unchanged map behavior); `verify-menu-icons-flags.mjs` 16/16; all 24 `verify-menu-fit.mjs` combinations; `npx tsc --noEmit`; and `npm run build`. Final screenshots: `output/dongho-life/menu-844.png` and `output/dongho-life/menu-664.png`.
- **Bamboo replacement, calm arc-length stream, and lotus water response.** Removed the disputed house plate and every house-only cue, including hearth smoke. In its place, a true-alpha right-bank bamboo screen uses the simple carved contours, restrained natural-pigment blocks, and dark final outline documented for Đông Hồ prints; the image was generated against the existing mountain and lotus plates, isolated on `#00ff00`, spill-cleaned, measured (`590..1494 × 448..970`), scaled to 48%, and registered below the mountain line. River currents now use uniform arc-length sampling and need 48–58 seconds to traverse the spline; measured travel fell from 6.7px to 3.15px per 260ms on the tall menu and 2.20px compact. Lotus hover/drag now emits its own tagged wake beneath the flower, adds a ripple while pressed, and still drives the damped bend/return spring. Verification: required web-game client state and 390×620 capture visually reviewed; `verify-living-map.mjs` ALL PASS (lotus wake source, current timing/interpolation, exact bamboo alpha bounds, compositing and unchanged gameplay map); `verify-menu-icons-flags.mjs` 16/16; all 24 `verify-menu-fit.mjs` combinations; `npx tsc --noEmit`; and `npm run build`. Final screenshots: `output/web-game/menu-bamboo-stream-lotus-final-2/shot-0.png`, `output/dongho-life/menu-844.png`, and `output/dongho-life/menu-664.png`.
- **Rear-dike bamboo, local river shimmer, and distinct mountain mist.** Reduced the bamboo plate from 48% to 20% and moved it to source-space `1400,480`, where it reads as a distant windbreak on the upper-right outer dike rather than a foreground grove planted inside rice. Removed the full-channel traveling current marks that looked like sliding scratches; the authored river is now calmed by a three-band river-only paper glaze and four restrained local surface shimmers that move no more than two pixels. Rebuilt the atmosphere as five separate blue-grey-edged wisps crossing the actual mountain feet over 6.6–9.7 seconds. Verification: required web-game pointer/drag client run and tall/compact captures visually reviewed; `verify-living-map.mjs` ALL PASS (5/5 mist wisps physically moved over 900ms, all four shimmers moved with ≤0.53px sampled displacement, three river veil bands, lotus wake and unchanged gameplay map); `verify-menu-icons-flags.mjs` 16/16; all 24 `verify-menu-fit.mjs` combinations; `npx tsc --noEmit`; and `npm run build`.
- **Planted-field continuity correction.** Moving the bamboo exposed a conspicuous orange-brown bare polygon baked into `menu-layer-ground-v3.png`; it was not a natural gap and should not have passed the prior screenshot review. Edited only that plot into pale flooded soil with perspective-aligned rice seedlings matching its neighbours, preserved every dike and river contour, and integrated the result non-destructively as `menu-layer-ground-v4.png`. The ground now declares `fully-planted-rice` and the living-map regression requires that version and continuity marker. Verification: required client pointer run and 390×620/390×844 images visually reviewed; `verify-living-map.mjs` ALL PASS with no console errors; menu controls 16/16; `npx tsc --noEmit`; and `npm run build`.
- **Visible lotus stem-waterline wake.** The earlier implementation created a tagged wake but incorrectly projected lotus touches onto the nearest point of the river centre spline, so the effect could appear away from the flower or be hidden beneath a leaf; the old test asserted existence only and missed the visual failure. Added two flower-specific stem anchors and a dedicated expanding three-ring waterline wake. Hover produces a readable response, press adds a stronger ripple, and drag nudges/overlaps the rings while all water remains composited beneath the lotus. `verify-living-map.mjs` now asserts the wake origin, active tween, and normalized physical placement (`x 0.15..0.38`, `y 0.88..0.94`) at both menu heights. Verification: required client touch/drag capture visually reviewed at `output/web-game/lotus-stem-wake/shot-0.png`; full living-map suite ALL PASS with no console errors; menu controls 16/16; `npx tsc --noEmit`; and `npm run build`.
- **Readable slow river flow.** The local-shimmer correction had become too restrained to survive phone-scale rendering: it began halfway down the channel, used only 4–6 marks, and nested alpha reduced the effective contrast to near invisibility. Extended ambient coverage from river anchors `0.18..0.95`, increased the set to 6 low-quality / 8 normal-quality bands, reduced the paper veil so the authored water texture remains readable, and replaced continuous strokes with broken pale reflection patches that widen, fade, and move locally. They now travel only `2.8..4px` over `6..9s`; the living-map measurement records all 8 moving at `0.60px/900ms` with capture alpha `0.33`, preserving a calm stream rather than fast scratches. Verification: required client captures visually reviewed at `output/web-game/visible-slow-stream-2/shot-0.png`; full living-map suite ALL PASS including lotus waterline wake and map behavior; menu controls 16/16; no console errors; `npx tsc --noEmit`; and `npm run build`.
- **Layered bamboo wind + reflection-only river graphic.** The grove previously translated as one rigid image by only `0.7px / 0.16°` across 11.8 seconds, which was technically animated but visually static. Kept the root-bearing base registered on the rear dike and added two exact cropped overlays from the same true-alpha plate: upper culms sway gently, while the leaf canopy moves farther with delayed phase. The river no longer draws any animated contour strokes: a two-tone blue-grey/warm-paper channel wash defines the water, and eight broken translucent light pools widen, fade, and drift locally across it. `verify-living-map.mjs` now requires both wind layers, asserts both physically move over 900ms at 844px and 664px, and requires the reflection-only water graphic. Verification: required client captures visually reviewed at `output/web-game/bamboo-wind-water-reflections-2/shot-0.png`; full living-map suite ALL PASS (2/2 bamboo wind layers and 8/8 water reflections moving, lotus wake/map unchanged); menu controls 16/16; no console errors; `npx tsc --noEmit`; and `npm run build`.
- **Softer lotus motion.** Reduced the flower's idle breeze from a noticeable multi-pixel sway to roughly one pixel of slow travel, capped touch/drag bending at `2.2px × 1.1px` and `0.7°`, and replaced the springy overshooting return with a longer sine-eased recovery. The existing stem-waterline wake remains visible and unchanged. `verify-living-map.mjs` now measures the gentle motion profile and hard response limits at both menu heights. Verification: required pointer/drag client capture visually reviewed at `output/web-game/soft-lotus-motion/shot-0.png`; living-map suite ALL PASS with no console errors; menu controls 16/16; `npx tsc --noEmit`; and `npm run build`.
- **Visible natural river and lotus motion.** The prior softness pass left the ambient flower and river reflections effectively sub-pixel for most of their cycles. Restored the flower to a middle-range `2px × 1.6px / 0.18°` idle breeze over 5.8 seconds—still clearly below the rejected `2.5px × 2.8px` version—and expanded each broken river reflection to `7..9.4px` of sine-eased travel over `7.2..10.42s`. Measured at the compact menu, river highlights move up to `1.03px/900ms` and the flower moves `0.51px/900ms`; touch/drag limits and the stem wake are unchanged. Verification: four-frame ambient sequence and touch/drag captures visually reviewed under `output/web-game/visible-natural-motion*`; `verify-living-map.mjs` ALL PASS at both menu heights with no console errors; menu controls 16/16; `npx tsc --noEmit`; and `npm run build`.
- **Alive, directional river surface.** The prior larger cream highlights still disappeared into the paper texture and did not communicate flow. Rebuilt the animation as three coordinated water cues beneath the lotus: 8 rounded indigo-watercolour pools that drift and fade forward, 7 staggered expanding surface rings, and 9 small paper-light reflections that follow the authored S-curve downstream in `46..55.6s`. Avoided both failed graphic directions encountered during visual iteration: long tangent-aligned blue streaks and channel-crossing light shafts. At compact size, all 9 directional flecks move (maximum `5.31px/900ms`) while the broader pools remain calm (`1.61px/900ms`). Verification: required multi-frame and touch/drag captures visually reviewed at `output/web-game/alive-river-downstream-final/` and `output/web-game/alive-river-final-touch/`; `verify-living-map.mjs` ALL PASS at both menu heights with no console errors; menu controls 16/16; `npx tsc --noEmit`; and `npm run build`.
- **Bank-to-bank river animation spread.** The alive-water effects all had distinct positions along the river but zero lateral offset, so they collapsed into the single centre rail visible in user feedback. Added a perspective-aware cross-channel coordinate whose half-width grows from `1.2%` of the artwork near the mountains to `16%` near the lotus. Assigned balanced signed lanes to all 8 pools, 7 pulse rings, and 9 downstream flecks, plus a small lane wander for organic flow. Traversal duration now scales with rendered artwork width so tall and compact menus keep the same perceived speed instead of accelerating on wider layouts. `verify-living-map.mjs` requires left/centre/right lane coverage for every water cue; measured downstream movement is `5.94px/900ms` at 844px and `6.49px/900ms` at 664px. Verification: required multi-frame captures visually reviewed at `output/web-game/river-bank-spread-final-2/` and `output/web-game/river-bank-spread-responsive/`; living-map suite ALL PASS with no console errors; menu controls 16/16; `npx tsc --noEmit`; and `npm run build`.
- **Strict Đông Hồ conquest-map asset pack.** Inventoried 316 world-art variants and reviewed generated replacements family by family. Accepted 270 true-alpha sprites into `public/art/conquest-dongho/`; retained 46 procedural fallbacks (all 15 Nguyễn Lord and 15 Yuan figure variants after three failed transparency outputs, plus 16 terrain plates that repeated visibly or could not clip safely). The typed manifest preloads accepted assets only, preserves procedural geometry/ordering/culling, and exposes `?mapart=procedural` for baseline or rollback. Runtime replacements cover seasonal flora, settlements and five citadel eras, atomic buildings, paddies/karst/ridges, people/animals/traffic, flags/markers, and 11 complete army wardrobes. Masters, rejected attempts, decisions, prompts, family sheets, one 316-entry contact sheet, fixed-seed old/new captures, comparison sheet, alpha audit, and fallback audit live under `output/conquest-dongho-review/`. Verification: 270/270 PNGs have real transparency and no chroma residue; missing and corrupt settlement sheets both fall back on a live 42-land map; TypeScript, 9/9 ground-scale, all seasons (weakest consecutive change 34.1%), world sharpness 9/9, culling, living-map, 15/15 smoke, production Vite/service-worker build, all 26 offline/PWA checks, and the 250ms seasonal budget (225.9ms under 4× CPU throttle) pass.
- **Actual gameplay integration recheck.** Drove the real menu and Dragon Ascent setup through the required Playwright canvas client, then captured the fixed-seed live Phaser map after onboarding. The cropped pack is visibly active in gameplay for citadels, farmstead/mine composites, constructed props, farmers/travellers, buffalo and ox carts, army ranks, standards, seasonal flora, karst and paddy overlays. All seven land types plus the army capture completed with no console errors; screenshots are under `output/conquest-dongho-review/actual-game-runtime/`.
- **Conquest-map projection, palette, and real-size correction.** Rejected the first saturated calibration and locked the accepted `style-board-v2.png` to the game’s pale điệp paper, softened umber line, straw, muted bamboo green, blue-grey, restrained son red, and low-contrast print density. Used the third/final per-variant output to rebuild the complete 4×3 settlement sheet and 6×5 building/prop sheet with one fixed 30° oblique isometric camera, shared bottom anchors, and one implied door/person scale. Added a five-state rice family (flooded, fallow, transplanted, ripe, nursery) with identical anchors and crop-only state changes; deterministic post-processing normalizes every accepted runtime sprite toward the exact procedural pigment table, opacity by family, real alpha, and a strict 2:1 rice footprint without spending a fourth generation attempt. Runtime metadata now declares projection and explicit size multipliers; villagers/travellers/soldiers share one standing band, buffalo remain below it, carts and building glyphs no longer fill their source boxes, village composites vary only 2%, and five citadel eras vary 8% while remaining slightly larger than villages. Removed rice mirroring so the entire field system keeps one camera/light direction. Final masters are `masters/final-settlements-isometric-v3-green.png`, `masters/final-buildings-isometric-v3-green.png`, and `masters/final-rice-fields-isometric-v3-green.png`; final fixed-seed captures are in `output/conquest-dongho-review/consistency-final/`.
  - Verification: visually reviewed the 316-entry all-assets contact sheet, family sheets, four seasons, and the final procedural/new comparison. New `verify-conquest-art-visual-scale.mjs` passes 12/12 (farmer 8.3px, traveller 8.4px, soldier 9.5px, buffalo 8.7px; all paddies exactly 60×30); 270/270 runtime PNGs retain real transparency with zero chroma residue; missing/corrupt fallback passes; TypeScript/build clean; ground scale 9/9; sharpness 9/9; culling; living map ALL PASS; smoke 15/15; seasons weakest consecutive difference 33.1%; season repaint 190.1ms under the 250ms 4×-CPU budget; PWA 26/26. Generated conquest sprites are install-optional in the service worker (48 critical shell files + 549 optional art files), because every one retains its procedural fallback.
- **Flag and circular-marker crop correction.** The full flagpoles, tassels, and circular seals were present in the accepted marker master; the loss came from extracting a nominal 256px grid even though the generated sheet's artwork crossed those nominal gutters. Added measured custom row/column edges for the marker sheet and guaranteed transparent padding after full-alpha trimming, preserving the existing Đông Hồ drawing and palette without spending another ImageGen attempt. Rebuilt all 23 marker variants; all twelve player/rival flags now retain their complete tall silhouette and every flag or circular icon has zero visible pixels touching its runtime crop edge. Verification: `verify-conquest-art-marker-crops.mjs` passes 5/5, visual-scale passes 12/12, missing/corrupt fallback passes, the required canvas-client run and fixed-seed gameplay captures have no console errors, and `npm run build` succeeds with the corrected optional art in the service worker.
- **Structure-only isometric architecture correction.** Replaced the settlement and building masters with v5 sheets drawn from the same south-west dimetric camera. All 39 accepted architecture sprites now carry a `structure-only-transparent` contract and contain no baked people, animals, tile ground, water, fields, trees, or background; empty courtyards and interiors remain alpha so the procedural map tiles show through. The generated mine worker was rejected from the building family, while settlement farmers and travellers are separate live sprites that walk within bounded compounds and turn with their direction. Five citadel eras were re-audited against institutional Vietnamese heritage references and retained only as structure silhouettes. Verification: structure isolation 7/7 (seven independent walkers, zero baked), citadel history/camera 7/7, visual scale 12/12, marker crops 5/5, missing/corrupt fallbacks pass, living-map ALL PASS, production build clean, and the complete offline/PWA suite passes. Reviewed contact sheets are `output/conquest-dongho-review/contact-buildings.jpg`, `contact-settlements.jpg`, and `contact-citadels.jpg`; fixed-seed 1.4x/2.4x gameplay captures are under `output/conquest-dongho-review/structure-only-runtime/`.
- **Complete four-pose army replacement v2.** Rejected the previous mixed five-pose army sheets and rebuilt all 13 wardrobes against `docs/resources/army/timeline-of-vietnamese-military-v0.webp` plus the written foreign wardrobe contracts. Each wardrobe now has exactly three levels and four reviewed visual cells in the fixed order spear, sword+shield, bow-or-matchlock, horseman. All source figures are flat side-on 2D, face viewer-right, use a planted ready stance, and share one baseline/body scale. The runtime keeps the game’s internal `skirmish` and `bow` slots by deriving both from the one reviewed ranged cell, yielding 156 distinct generated figures and 195 compatible runtime IDs. Every figure exports on the same 144×128 transparent canvas; map scale is 1.51 for foot figures and 1.18 for mounted figures, keeping soldiers at 8.3px beside 8.3px farmers. Lý used its third and final attempt; all other wardrobes passed on their first v2 output. Final masters are `output/conquest-dongho-review/masters/alpha-army-v2-*.png`; the normalized all-army review is `output/conquest-dongho-review/contact-armies-v2.jpg`; the real Battle Arena capture is `output/conquest-dongho-review/army-v2-facing-skirmish.png`.
  - Verification: army matrix 17/17 checks (195/195 runtime IDs, 156/156 unique visual cells, byte-identical ranged aliases, fixed source order/canvas, correct attempts, native-right facing, arena mirroring and aligned feet); alpha audit 299/299 with real transparency and zero chroma fringe; visual scale 12/12; ground scale 9/9; world sharpness 9/9; culling ALL PASS; structure isolation 7/7; missing/corrupt fallback PASS; marker crop 5/5; TypeScript + production Vite + service-worker build clean; PWA/offline 26/26. The standalone battle-scale verifier currently targets a stale `ConquestUIScene.battleUi` initialization path and exits before measuring; the army-specific real Battle Arena test is green.
- **Vietnamese army clothing correction v3.** Re-audited and replaced clothing only for the eight Vietnamese wardrobes—Đinh, Lý, Trần, Lê, Trịnh, Nguyễn Lords, Tây Sơn, and Nguyễn dynasty—while preserving the accepted v2 pose, fixed spear/sword+rattan-shield/ranged/horseman order, viewer-right native facing, planted ready stance, grid, baseline, weapon geometry, and runtime size. Period-specific corrections include the Đinh square-topped cap vocabulary, clearer Lý/Trần/Lê armor separation, woven Trịnh service headwear, post-1744 Nguyễn Lords front-fastened long coats and trousers, Tây Sơn headcloth and southern tunic/broad-trouser silhouettes, and Nguyễn `áo song khai` with low service hats and restrained later-period armor. Song, Yuan, Ming, Qing, and Champa sheets remain unchanged and explicitly marked foreign/out of this clothing pass. Accepted transparent masters are `output/conquest-dongho-review/masters/alpha-army-v3-clothing-*.png`; rejected/raw variants remain under `masters/clothing-v3-attempts/`; the normalized 96-cell review is `contact-vietnamese-clothing-v3.jpg`; and the real arena capture is `army-clothing-v3-facing-skirmish.png`. Review manifest, prompts, decisions, attempt counts, period notes, and evidence basis are recorded in the existing JSON audit files.
  - Verification: army matrix all checks PASS (195/195 runtime IDs, 156/156 unique visual cells, all eight clothing-only revisions, native-right sources, correct arena mirroring, aligned feet, fixed 144×128 canvases, ranged aliases and size compensation); all 299 accepted runtime PNGs retain real transparency and zero chroma fringe; visual scale 12/12 (soldier/farmer both 8.3px); marker crops 5/5; citadel/history camera 7/7; structure isolation 7/7; missing/corrupt fallbacks PASS; ground scale 9/9; world sharpness 9/9; culling ALL PASS; living map ALL PASS; battle ground bake 5/5; smoke 15/15; TypeScript + production Vite + service-worker build clean; PWA/offline 26/26. Under 4× CPU throttle, a normal seasonal repaint remains within budget at 166.7ms of 250ms.
- **Mounted-figure crop correction.** The complete horses were present in every accepted army master, but the extractor cut the mathematical fourth grid cell even though the wider mounted drawings deliberately crossed its left boundary; trained and royal riders also crossed nominal row dividers. This permanently removed the horse's rear and sometimes the rider's head before Phaser rendered it. Mounted extraction now uses an expanded measured window, removes only neighbouring ranged fragments entering through its edge, preserves the complete connected silhouette, and normalizes it uniformly onto the same 144×128 canvas with a shared y=123 foot line. Runtime scale 1.33 is the reciprocal compensation for the wider source board, keeping rider height unchanged. All 39 mounted variants across all 13 wardrobes now have transparent safety margin on every edge; their manifest records the full mounted design bounds and `expanded-complete-mounted-silhouette` crop policy.
  - Verification: visually reviewed all 156 army cells in `contact-armies-v2.jpg`, all 96 Vietnamese clothing cells, three representative runtime PNGs at original resolution, the real arena preview, and a real 1,500-vs-1,500 Conquest battlefield formation at phone scale (`output/web-game/battlefield.png` and `battlefield-crop.png`). Army matrix 19/19 PASS including the new 39/39 mounted-edge assertion; visual scale 12/12; ground scale 9/9; required web-game client capture reviewed; smoke 15/15; TypeScript + production Vite + service-worker build clean; offline/PWA 26/26; no console errors.
- **Strict front-centred 30° structural replacement.** Rejected the earlier south-west dimetric architecture and rebuilt/accepted exactly 45 non-living structural assets under one explicit camera contract: horizontal yaw 0°, orthographic vertical elevation 30° downward, depth travelling straight upward with no perspective convergence. The set contains 28 atomic buildings/props, 11 settlement composites including five historically differentiated Đại Việt/Vietnamese citadels, five same-footprint rice states, and one timber bridge. Every individual component—not merely each overall composite—faces squarely forward; buildings contain no baked people, animals, tile ground, or background. Runtime metadata now distinguishes `front-orthographic-30` / `front-centered-elevation-30` from the untouched flora projection, and all files retain procedural fallback plus `?mapart=procedural` rollback. Full-resolution masters are in `output/conquest-dongho-review/masters/front-centered-vertical30-v3/`; runtime PNGs are in `public/art/conquest-dongho/`.
  - Verification: all-at-once art and ground-scale sheets reviewed (`contact-front30-all.jpg`, `contact-front30-ground-scale.jpg`); real fixed-seed new/old comparison reviewed at 2.4× (`comparison-front30-procedural-gameplay.jpg`); 45/45 camera/style gates and alpha/chroma audits pass; five rice states vary only 1.5% in height and 0% in width; five citadels vary 1.09× in runtime height; structure isolation 7/7; history/camera 7/7; scale 12/12; fallback missing/corrupt PASS; armies 19/19; ground scale 9/9; world sharpness 9/9; culling and living-map ALL PASS; four seasons weakest change 33.1%; seasonal repaint repeated green at 210.7ms/250ms under 4× CPU throttle; TypeScript/Vite/service-worker build clean; offline/PWA 26/26; no console errors in gameplay captures.
- **Strict elevation-30 visual repair after user screenshot review.** Rejected the earlier metadata-only interpretation of the camera and required visible geometric proof: broad roof upper planes, platform/floor tops, multiple stair-tread tops, wall-walk/parapet tops when present, compressed facades, and zero horizontal depth drift. Rebuilt 31 failing or weak assets: three primary village buildings, the five-tier pagoda, three rural structures, seven constructed improvements, all six settlement composites, all five citadel eras, all five rice states, and the timber bridge. Fourteen open/round/deck-based props were retained only after individual review because their existing curb interiors, fence depth, decks, basket rims, or horizontal top planes already proved the camera. The first pagoda repair was rejected despite passing camera because it read as a generic ziggurat; its final third output restores a slender five-stage Vietnamese brick tower while preserving deep visible tier tops. Attempts, rejection reasons, prompts, and source paths are recorded under `output/conquest-dongho-review/attempts/elevation30-fix-2026-08-30/` and in `prompts.json`.
  - Verification: complete building, settlement, terrain, and shared-ground-scale sheets visually reviewed; actual fixed-seed gameplay reviewed at 1.4×/2.4× for all seven land types with no console errors (`playtest-elevation30-final/`); 39/39 architecture sprites are structure-only transparent with independent living people; 45/45 structural camera declarations and all alpha/chroma checks pass; village families share one height, citadel heights vary only 1.03×, and all rice footprints are identical; structure isolation 7/7, visual scale 12/12, citadel/history 7/7, marker crop 5/5, missing/corrupt fallback PASS, review JSON valid, and the production TypeScript/Vite/service-worker build passes. No people or animal assets were changed in this repair.
- **Top-dominant yaw-0/elevation-30 correction after second screenshot audit.** The previous gate still allowed shallow frontal elevations, so the accepted camera proof is now visual rather than metadata-only: large roof/deck upper planes dominate, façades are strongly shortened, stair and platform tops remain visible, and compounds are bilaterally centered with no left/right yaw. Rebuilt 27 weak structural variants including the user's pictured houses, kitchen, byre, grain bin, market/mine/tower/workshop/guild improvements, hamlet/village/market/shrine/farmstead composites, the pagoda, and all five Đại Việt/Vietnamese citadel eras. Rejected a farm improvement with a baked slab, a wall with an enclosed courtyard slab, a farmstead with an invented fence, and the first Đinh repair with excessive side-wall convergence. Accepted/rejected masters and audit live under `output/conquest-dongho-review/attempts/top-dominant30-fix-2026-08-30/`; the deterministic installer is `scripts/conquest-art/install-top-dominant30-fixes.py`.
  - Fixed the alpha extractor after real gameplay exposed painted checker cells trapped inside enclosed citadel courtyards. Near-white checker cells now seed a neutral-only flood that clears the courtyard and its antialias fringe while preserving warm straw, brick, wood, and stone pigments. Procedural field tiles now show through every compound gap.
  - Verification: visually reviewed the regenerated 45-entry all-assets sheet, building and settlement sheets, original-resolution citadel PNGs, and the fixed-seed game at 1.4×/2.4× for all seven land types. Final capture is `output/conquest-dongho-review/playtest-top-dominant30-alpha-final/` with no console errors. Structure isolation passes 7/7, visual scale 12/12, citadel/history 7/7, review JSON is valid, and the final TypeScript/Vite/service-worker production build succeeds. People and animals remain untouched.
- **Natural Vietnamese settlement and scale correction.** Audited all 45 strict front-30 structural assets individually and recorded keep/scale-tune/regenerate decisions in `output/conquest-dongho-review/natural-structure-audit-2026-08-31.json`. The live-placement cause of the reported robot columns was not the artwork: constructed buildings were positioned from the province centroid rather than the valid settlement seat. Additions now remain in one deterministic irregular work envelope around the seat, with subtle connecting lanes; a citadel never receives a duplicate detached wall, and an unwalled settlement receives one complete enclosure rather than a floating gate.
  - Rebuilt the six non-citadel composites around practical Vietnamese circulation and work relationships: staggered hamlet/village lanes, shop houses around a market lane, a connected shrine boundary, hay beside the byre and grain beside the dwelling, and an adit/hoist/storage handling space. Rebuilt the bamboo hedge as one continuous open-front boundary and the wall as a complete transparent-courtyard enclosure. No regenerated structure contains people, animals, terrain, ground slabs, or opaque white/checker backgrounds. The accepted pass and rejected attempts are under `attempts/natural-village-scale-2026-08-31/`; deterministic installation is `scripts/conquest-art/install-natural-village-structure-fixes.py`.
  - Runtime scale is now measured rather than guessed: constructed improvements are 21.5px high against an 8.3px soldier; all six village families are 72.7–76px; all five citadel eras are exactly 76px. The final new-assets sheet is `contact-natural-village-fixes.jpg`; all 45 assets together are in `contact-front30-all.jpg`; live fixed-seed 1.4×/2.4× captures are under `playtest-natural-layout-final-normalized/`.
  - Verification: structure isolation 7/7, visual scale 12/12, citadel/history 7/7, marker crops 5/5, missing/corrupt fallback PASS, all runtime structural PNGs have true alpha with no chroma fringe, world sharpness 9/9, culling ALL PASS, living-map ALL PASS, battle ground bake 5/5, smoke PASS, TypeScript and production Vite/service-worker build clean, and the complete production preview PWA/offline suite passes with no console errors.
- **Semantic world-scale and collision system.** Replaced source-box-driven structure sizing with typed semantic world-height contracts. Individual constructed buildings are 13px high, houses 15px, rural compounds 34–38px, towns 44–52px, karst ranges 56px, and citadels 76–86px at design scale; source aspect ratio now controls width only after the class height is applied. This fixes shallow village plates outgrowing capitals and mountains shrinking beneath houses, and records the contract in both runtime/review manifests through `sync-world-scale-contracts.mjs` so future assets cannot silently inherit arbitrary generated dimensions.
  - Added measured settlement-footprint planning shared by every settlement family. Improvements avoid the compound, other improvements, nameplate keep-outs, water, and mountain tiles; roads connect to the resolved positions; citadels cannot receive a second wall; mountain scatter reserves each settlement's measured clearance. Labels are resolved globally after all provinces exist, capitals have priority, moving traffic is hidden behind relief/nameplates, and the search expands deterministically until a collision-free position exists instead of accepting an overlapping fallback.
  - Verification: semantic scale 16/16; all 11 settlement families × 10 constructed additions place with zero collisions; the fully revealed fixed-seed map reports 42 labels / 35 structures / 0 overlaps; structure isolation 7/7; missing/corrupt fallback PASS; world sharpness 9/9; culling, ground scale, living map, and land taps PASS; TypeScript and production Vite/service-worker builds pass; PWA/offline 26/26; seasonal repaint 235.7ms under the 250ms 4×-CPU budget. Final 1.4×/2.4× captures are in `output/playtest/`; the machine-readable audit is `output/conquest-dongho-review/world-scale-layout-audit-2026-08-31.json`.
- **Natural terrain variety, clean village roads, and stable map markers.** Added four reviewed Vietnamese tree silhouettes (jackfruit, lychee, pomelo, and silk-cotton) beside the accepted original and five reviewed limestone silhouettes. Every seasonal tree uses one 144×144 bottom-centred canvas, every karst uses one 240×160 bottom-centred canvas, and deterministic seed selectors reach all five without moving when the season repaints. The accepted capped rice plates remain at their third-output limit; the runtime now draws those 384×181 generated plates at readable opacity and invokes the canvas field renderer only when an image is unavailable, rather than painting canvas bunds over the generated art. All natural art is normalized to the game pigment table and true padded alpha; the 30-entry consistency sheet is `output/conquest-dongho-review/contact-natural-terrain-variants.png`, with accepted/rejected masters and decisions under `attempts/natural-terrain-2026-08-31/`.
  - Authored settlements no longer retain the legacy inter-fortress-hex road mesh underneath their transparent courtyards. Constructed-building lanes leave the real front gate and route around the measured compound footprint instead of cutting straight through it; inter-province roads, carts, and travelers use a stable gate endpoint plus a short exterior runway before turning toward the next province.
  - Fixed map progress icons at both causes. Battle/siege pulse tweens now multiply the fitted stamp scale instead of overwriting it with an absolute 1.09 (the source of the giant icon), and no icon animation changes x/y. Progress badges now use one fixed world point above the settlement instead of averaging whichever province hexes happen to be inside the camera, so panning and zooming cannot make them wander. All five glyphs measure 15.6–17.3px through their full animation, all 23 marker PNGs keep transparent crop padding, and the final two-timepoint visual check is under `output/conquest-dongho-review/marker-runtime-final/`.
  - Verification: natural terrain/marker contract 12/12; marker crop 5/5; all 320 accepted runtime files pass alpha audit; semantic visual scale 16/16; 11 settlement layouts pass with zero satellite/label collisions; missing and corrupt art fall back procedurally; ground scale 9/9; world sharpness 9/9; culling ALL PASS; living map ALL PASS; TypeScript, Vite, and service-worker production builds pass; offline/PWA 26/26; season turn 131.1ms under the 250ms 4×-CPU budget. Fixed-seed 1.4×/2.4× captures for all land types are in `output/conquest-dongho-review/playtest-natural-markers-final/`.
- **Natural farmer/cart/buffalo locomotion complete.** Authored `life.ox-cart`, buffalo-family, and farmer sprites now declare their real viewer-right orientation while procedural fallbacks retain their viewer-left convention. Road carts, bounded grazing buffalo, and settlement farmers gain restrained travel-only bob/sway cycles that settle during stops and pause with the world or view culling; tightened direction dead zones also prevent slow near-vertical travel from retaining the previous facing. The focused live-map audit passes 16/16 with every uninterrupted sampled step correct (cart 209/209, buffalo 510/510, farmer 390/390), active gait motion, and no browser errors. Broader `verify-living-map.mjs` is ALL PASS (including both cart route legs, bounded herds, batching, ground order, and zero console errors); TypeScript, production Vite, service-worker, and required gameplay-client checks pass. Close-up review captures: `output/web-game/natural-moving-sprites/cart-moving.png`, `buffalo-moving.png`, and `farmer-moving.png`.
- **Real articulated walk frames complete.** Whole-image movement/bob is no longer used for authored walkers. Settlement farmers, road/settlement travellers, grazing buffalo, and ox carts now select non-destructive 2x2 four-pose sheets beside their original still assets; feet/hooves alternate, bodies counter-move, and cart wheel spokes advance. Playback is driven by actual world distance, each pose is pinned to its measured ground baseline, and left/right mirroring follows the measured travel direction. The screenshot follow-up exposed the separate `life.traveler` road path, so it received its own faithful four-frame sheet and explicit viewer-right native facing as well. Procedural and missing-art fallbacks keep the old restrained rigid motion.
  - Verification: all four sheets pass 16/16 alpha, padding, cell-grid, and pose-uniqueness checks. The focused live-map audit passes 21/21: all four frame indices appear with zero authored whole-image bob; sampled direction is correct for cart 201/201, traveller 1675/1675, buffalo 494/494, and farmer 375/375; no browser errors. `verify-living-map.mjs` is ALL PASS; TypeScript, production Vite, service-worker, and the required gameplay-client checks pass. Per-frame captures and the machine-readable audit are in `output/web-game/natural-moving-sprites/`.
  - **Cart map-scale readability follow-up.** The four cart poses were advancing, but the 1.8-world-unit frame stride let the dominant cabin remain unchanged long enough that the small rig still read as a sliding still in user gameplay. Kept the accepted true-alpha sheet and shortened only its distance-per-frame stride to 0.55, so the buffalo hoof cycle remains movement-synchronised but is now readable at map size. Several stronger ImageGen pose variants were correctly rejected because their checker/black/white backgrounds were baked opaque; none entered runtime. The live audit now requires repeated automatic cart frame transitions and passes with 129 sampled changes across nine moving carts, frames 0–3, zero whole-image bob, and 193/193 facing samples correct. The 16/16 sheet audit, 21/21 mover audit, living-map suite, TypeScript, Vite, service-worker, and required client all pass.
  - **Between-land farmer/traveller readability follow-up.** The user's moving “farmer between lands” is the `life.traveler` road role. It already selected the four-frame sheet, but the road-specific 18x20 fit reduced it to about eight world pixels tall—too little resolution for alternating feet to survive map texture filtering. Expanded only the road traveller fit to a restrained 13–15px silhouette and shortened its distance-per-frame stride from 0.9 to 0.45. The focused audit now enforces readable road height and passes 22/22: 14.8px tall, 1,234 automatic frame changes across 80 moving travellers, frames 0–3, zero whole-image bob, and 1,680/1,680 facing samples correct. All four gameplay-frame captures were visually reviewed; sheet audit, living-map suite, TypeScript, Vite, service-worker, and required client pass.
- **Conquest army frame animation on map and battlefield.** Replaced the Conquest-only stamped host's old whole-rank/whole-marker bob with real four-frame contact and passing poses made from overlapping crops of each accepted soldier asset. Infantry alternate two independently pivoted feet; mounted ranks animate four horse-leg sections; torso counter-motion is restrained; rank/figure phase offsets prevent synchronized rubber-stamp movement. The shared `DongHoMapItemRenderer` path gives the same rig to campaign-map hosts and every Conquest battle block, including enemy mirroring and post-casualty rebuilds. The map route container stays planted instead of hopping, while the procedural-art rollback retains its former rank cadence. Legacy map-item renderers/gameplay were not changed.
  - Verification: new `verify-conquest-army-animation.mjs` passes 10/10 across the actual Conquest map and battle UI—four distinct map poses, all 54 sampled battle figures articulated including 8 mounted, correct opposing facing, and animation restored after an attrition rebuild. Army assets/mirroring pass 19/19, natural movers 22/22, map host 6/6, battle start 8/8, battle scale and ground-bake suites pass, living-map ALL PASS, authored-art fallback passes, no browser errors, and TypeScript + production Vite + service-worker builds are clean. Gameplay captures reviewed: `output/army-animation-map/` and `output/web-game/battlefield.png` / `battlefield-crop.png`; required gameplay-client capture also passes.
- **Road traveller opposite-foot correction.** Replaced `life/traveler-walk.png` with a true-alpha four-pose sheet whose two passing frames plant opposite left/right feet: frame 1 lifts the rear/viewer-left foot and frame 3 lifts the forward/viewer-right foot. Re-measured all four ground baselines, restored the road-only traveller to a 13.9px readable silhouette, and added a regression gate that measures the ground-contact centroid so arm-only frame changes can no longer hide an unchanged foot state. Added a character-safe checker-matte extractor that preserves pale skin and clothing highlights.
  - Verification: walk-sheet audit passes 17/17 including the new opposite-foot invariant; focused live-map mover audit passes 22/22 with 80 moving travellers, all four frames, 1,338 sampled transitions, 1,800/1,800 correct-facing steps, 13.9px height, and no browser errors. All four 3x gameplay captures were visually reviewed under `output/web-game/natural-moving-sprites/`; TypeScript, Vite production build, and service-worker build pass.
  - **Jitter/pose smoothing follow-up.** Replaced the exaggerated fourth-frame forward kick with a compact bent-knee opposite step and kept the shared 9.2px human world scale. Added measured per-frame torso anchors `[347, 284, 358, 288]` beside the foot baselines, removing more than 60 source pixels of cell-placement error that had made the body pop left/right. The road traveller no longer runs the old independent bob/tilt tween over its distance-driven articulated frames, eliminating the competing motion clocks visible in the user's screenshot; farmers, buffalo, carts, and procedural fallbacks retain their existing motion profiles.
    - Verification: sheet audit remains 17/17 with opposite ground contacts; focused live-map audit passes 22/22 with 80 moving travellers, 1,338 frame changes, 1,797/1,797 correct-facing samples, zero traveler whole-image bob, shared 9.2px height, and no browser errors. All four aligned 3x gameplay frames were visually reviewed; required gameplay client recovered from one early blank hot-reload frame and its final capture was reviewed. TypeScript, Vite production, and service-worker builds pass.
- **Mountain scale, scrub, and weather pass.** Kept the five reviewed transparent karst plates rather than regenerating their linework. Mountain scatter now plants only a new low, code-native evergreen bush (no tree and no missing authored-asset lookup), measured at 1.4 m / 0.868 scale. Karst height now starts at 1.72 tiles, grows only 0.2 per back row, and saturates at 2.32 before the authored silhouette fit; the fixed-seed map's tallest visible mountain ink is 3.23 tiles instead of a possible 4.39-tile input before silhouette overshoot. The same five images are seasonally multiplied through four pigment-safe weather colours, and the procedural fallback applies the identical colour transform; winter retains mist/snow accents.
  - Verification: focused natural-terrain/marker audit passes 18/18 with 38/38 mountain scatter marks as bushes, 79 authored mountain objects in every season, four distinct rendered tints with correct season metadata, 3.23-tile maximum visible ink, and no console errors. Bush geometry re-measures exactly at 5.0 units / UNIT 0.868. Four 390×844 gameplay captures were visually reviewed under `test_scripts/shots/season-dongho-{Spring,Summer,Autumn,Winter}.png`; every seasonal pair differs by at least 36.2%. Ground-order passes 5/5, authored missing/corrupt fallback passes, and TypeScript + production Vite + service-worker builds are clean. The broader ground-scale and semantic visual-scale suites still report pre-existing unrelated thresholds (living exaggeration 2.21× vs <2.20; traveller/citadel/projection checks); the mountain checks within them pass.
- **Mountain tree-overhang correction.** The bush-only source rule was not enough: trees, bamboo and other tall plants thrown from neighbouring forest/hill cells could still hang their crowns over a karst plate. Relief plans now retain their terrain kind, mountain clear zones account for a tall plant's reach while exempting low scrub, and the final scatter pass rejects the complete visible box of any tall vegetation that intersects authored mountain bounds. The five source plates were re-inspected and already contain only low base scrub, so no bitmap regeneration was needed.
  - Verification: the focused natural-terrain audit passes 18/18 with every mountain scatter mark identified as a bush and zero neighbouring tall-vegetation intersections across 79 authored ranges. TypeScript passes; all Spring/Summer/Autumn/Winter 390×844 captures were regenerated and visually reviewed with the seasonal mountain colours intact and no trees on the rock silhouettes. The required web-game client produced clean menu state/captures; the dedicated gameplay capture supplies the mountain view. Production Vite and service-worker builds pass.

## 2026-09-01 — historically grounded hero wardrobe and rendering pass

- Request: review `public/faces` and the hero-image renderer; improve clothing and hats; research Vietnamese historical dress; generate supporting art; use it in hero portraits.
- Baseline captured at `output/web-game/portraits-roster-{0,1}.png`; existing renderer is a 277-part SVG stack with 10–20 live Phaser Images per portrait and one SVG request/texture per part.
- Research correction found: the existing open-crown `khăn vấn` pool is too broad for Lý–Trần. Better-supported reads are Lý topknots/closed hair wrapping, Trần cropped or shaven heads, and the curved `mũ Đinh Tự` for Trần officials. Armor evidence remains interpretive and will stay explicitly caveated.
- Next: generate the neutral material study, add historically corrected headwear/garment rules, build a single textured atlas, bake each composed hero to one runtime image, then run contact-sheet and gameplay verification.
- Completed the wardrobe chronology pass: Lý heroes avoid the later open-crown Nguyễn headwrap; Trần ministers use the newly authored wired-brow `mũ Đinh Tự` (with rank streamers where appropriate) rather than Lý `Phốc Đầu`; pre-Lê court looks no longer receive `bổ tử`; Lê and Nguyễn retain period-appropriate rank dress; early women use hair, bands, coronets, or broad hats instead of later default wraps.
- Generated two neutral ImageGen material studies under `scripts/faces/materials/`: `handwoven-cloth.png` for ramie/hemp/plain-silk grain and `lacquered-gauze.png` for lacquered silk gauze/headcloth. The face build downsamples and clips these pixels inside the existing vector garment/headwear silhouettes, so generated art supplies surface variation without choosing chronology, rank, symbols, or shape.
- Rendering optimization complete: `scripts/build-faces.mjs` now packs 279 measured SVG parts into `public/faces/atlas.svg` plus `atlas.json`; preload uses two face requests and one GPU texture instead of one request/texture per part. `FaceRenderer` composes each deterministic hero once into a 1.5× render texture, then displays one Phaser Image with a layered fallback for context loss. Cache identity includes theme plus every look-driving hero field.
- Verification: face generation check passes at 279 parts / 2048×2048; TypeScript, production Vite, and service-worker builds pass; `verify-heroes.mjs` reports 127 roster heroes, zero missing parts, 522 distinct sampled looks, one runtime portrait child, two face requests, all five history invariants, and zero browser errors. Required gameplay-client captures and final roster/generated contact sheets were visually reviewed under `output/web-game/`; the existing Vite large-chunk warning is unchanged. Armor for Đinh and Tây Sơn remains deliberately interpretive because surviving evidence is thinner.

## 2026-09-01 — Vietnamese women's hair follow-up

- Request: improve female hero hair, which currently looks poor, using historical Vietnamese women's hairstyles.
- Baseline diagnosis: the resolver independently rolls a loose-hair part and a bun. This creates incompatible silhouettes—long rectangular side curtains combined with unrelated crown ellipses—and makes many women look like the same modern long hairstyle with an object placed on top.
- Research direction: use artifact-derived Lý–Trần fan, coil, side-loop, and compact crown forms; the documented Trần short crown-tied “brush” with no side/nape fall; Lê short neck-length or later loose long hair; and Nguyễn northern wrapped/smoothed hair or a low nape chignon. Twin side buns remain a narrow young/attendant interpretation, not a generic female default.
- Next: capture a focused baseline, author compatible one-piece hair-style pairs in the existing SVG system, gate them by era/covering/age, then rebuild and visually verify the atlas.
- Completed the hair architecture change. `womanHairStylesFor` now returns a complete front/rear arrangement plus a physical ornament anchor (`crown`, `brush`, `band`, `nape-left`, or `nape-right`). `heroLook` selects that arrangement once, suppresses pins under crowns/coronets/nón, and never rolls loose hair and a bun independently.
- Added 16 period-specific vector parts: smooth centre, face-framing, neck-short, restrained loose, wrapped-crown and compact Trần fronts; Lý–Trần fan, spiral, side-loop and crown-brush masses; mirrored subtle nape buns; and four side-matched nape pins. Rear masses render behind the head and front hairlines over it. The atlas now contains 295 measured parts at 2048×2048.
- Research is recorded in `docs/research/vietnamese-hero-wardrobe.md`: artifact-derived Lý–Trần fan/coil/side arrangements; Trần's short crown-tied brush and no side/nape fall; Lê neck-length hair plus later loose-long evidence; Nguyễn wrapped northern hair and subtle southern nape chignon. Đinh/Trưng and Tây Sơn remain explicitly conservative because their direct evidence is thinner.
- Verification: `build-faces --check`, TypeScript, production Vite, and service-worker builds pass. `verify-heroes.mjs` reports 295/295 atlas frames, zero missing parts, one baked runtime image, two face requests, twelve passing chronology/hair invariants, and zero browser errors. Women-only baseline/final sheets are `output/web-game/women-hair-{baseline,final}-{0,1}.png`; full roster/generated sheets were regenerated and reviewed. The required web-game client initially hit its headless WebGL blank-frame behavior, then passed visibly in `?capture=1` mode under `output/web-game/women-hair-client-final-capture/`. No remaining hair TODOs.

## 2026-09-01 — head-to-neck attachment follow-up

- Request: review portraits where the head does not connect correctly to the neck; supplied women-only sheet shows the issue across several jaw shapes.
- Diagnosis: all 16 heads share a randomly chosen 15px or 18px rectangular neck beginning at y=25, but head bottoms range from y=23.5 to y=31 because `tall` changes the chin as well as the crown. Short `head-round` can leave a visible paper gap, while narrow/heart/tapered jaws can expose an unrelated wide neck beside the chin. Neck width and head anatomy are currently independent random rolls.
- Next: raise and taper the shared neck geometry so it overlaps the shortest jaw, add a broad variant, couple neck width to the selected head family without perturbing the remaining portrait seed, then audit all head families visually and numerically.

## 2026-09-05 — Vietnamese royal-soldier crop and transparency repair

- Request: fix the non-transparent Đinh royal skirmisher and the severed/cropped heads across the Vietnamese royal-soldier sets, without inventing a shared ahistorical wardrobe.
- Repaired the four infantry roles for Đinh, Lý, Trần, Later Lê, Trịnh, Nguyễn lords, Tây Sơn, and Nguyễn; also cleaned the Đinh mounted cutout. The seven already-safe mounted files were retained. Each era keeps the wardrobe and headgear documented by the existing dynasty art direction rather than being homogenized.
- All 33 repaired files were fitted into the existing 144×128 contract with eight pixels of top and bottom headroom. A full 40-file audit confirms Format32bppArgb, real transparent pixels, and zero opaque edge pixels. The reviewed contact sheet was `royal-sprites-audit.candidate.png` before temporary-file cleanup.
- Verification: `verify-army-plate.mjs` passes 9/9 across 105 wardrobe/weapon/rank plates and 35 formations with no title/caption overlap or console errors. The required gameplay client passed and its menu capture/state were inspected under `output/web-game/royal-sprite-final-client/`; History army screenshots were regenerated and inspected in Vietnamese and English.
- No remaining royal-soldier crop or transparency TODOs.

## 2026-09-05 — Đông Hồ presentation, priorities 1 / 2 / 5

- Saved the full honest 6/10 pre-change review and its referenced images in `docs/graphics-review-2026-09-05/`. The implementation direction, final screenshots, limits and verification are in `docs/30-dong-ho-visual-language.md`.
- Improved map labels and progress counts with higher text resolution and capped apparent size above 1.15×. Hit geometry follows the visible label scale. Đông Hồ progress badges combine glyph/count/progress in one compact plate above measured settlement roofs; ownership hatching is quieter. The lower advisor signpost names the lane while the upper strip retains the event.
- Shared ink controls now use restrained contour variation/bleed, light flat paper, simpler corners and quieter framing. Three built-in ImageGen vignettes (rice collection, village muster, court petition) define a common reference family. Runtime WebP files total about 411 KiB under `public/art/story-prints/`; full prompts are in `docs/dong-ho-vignette-prompts.md`.
- Integrated prints into power card faces and their detail panel, recruitment, envoy, law and decree prompts. Existing semantic card symbols remain visible. Small screens retain card-face art and scroll the longer illustrated sheets. Measured the fan hint and recruitment caption to avoid crowded controls.
- Validation: production TypeScript/Vite/SW build passes; the three prints are in the offline shell list. `verify-dongho-presentation.mjs` passes all fitting, scaling, scrolling, actual pointer choice and missing-art checks in EN 390×844 and VI 390×844/620 at DPR2/medium, with no browser errors. Existing land taps 4/4, muster behavior 7/7, and 11 settlement layouts plus live label overlaps pass. Required web-game client screenshot and text state inspected under `output/web-game/dongho-final-client/`. Busy seeded turn66/wave4/four-province map inspected at 1.2× and 2.4× under `output/dongho-first-pass/`.
- Remaining art work: large map bakes still soften at close zoom; portraits and detailed terrain/soldier families still need further alignment with the shared print direction. Named historical scenes need separate researched compositions. No full-game post-change rescore or physical-device performance claim.

## 2026-09-05 — Đông Hồ soldier and building redraw (partial; full coverage still requested)

- Generated five master sheets with built-in ImageGen: Đinh, Lý, Trần, five citadels, six rural settlements. Exported 56 real-alpha PNGs into `public/art/conquest-dongho-v4/`. Selected 51 for runtime: 40 soldier slots (32 distinct drawings plus ranged aliases) and all 11 settlements. Withheld the five Đinh trained slots because the redraw lost chest armour; originals remain in use. The other ten wardrobes and all 28 accepted individual buildings still need redraws: 183 of 234 runtime entries remain.
- All screens share `src/ui/conquestDongHoV4Assets.json` through `conquestMapArt.ts`. Mechanical sheet/reference preparation, matte extraction, connected-silhouette export, review boards and normalized production prompts are in `scripts/conquest-art/dongho-style-pack.mjs`. Army canvases retain 144×128 for the existing rig; structures retain aspect ratio and semantic world size.
- Medium now renders authored structures and relief images live instead of losing their contours in the terrain cache. Low and cheap bake settings retain caching; expensive vector scatter remains cached on medium. Fixed rebake visibility restoring offscreen settlements by refreshing culling callbacks after the visibility sweep.
- Verified: production TypeScript/Vite/SW build, all 234 relevant runtime textures, pixel/alpha/edge/matte checks for all 56 exports, ranged aliases, 11/11 settlement layouts and zero live label overlaps, 13/13 map/battle animation checks using the new Lý/Trần sprites, History 9/9 (105 soldier plates/35 formations), ground order 5/5, missing/corrupt art fallbacks, and low/medium/high live/cached structure behavior including pan-away/return culling. Required client screenshot/text state inspected; actual new-art map at 1.2×/2.4×, History and battle frames inspected with no browser errors.
- Documentation and selected comparisons: `docs/dong-ho-soldiers-buildings.md`; all 18 normalized production prompts: `docs/dong-ho-style-v4-prompts.json`. Masters/references/review boards/gameplay captures and full pending-ID audit remain under `output/dongho-style-v4/`.
- Blocker: built-in ImageGen returned usage_limit_reached, reset 2026-09-05 19:53:59 UTC (2026-09-06 02:53:59 Bangkok). User said “Ok please continue” after the API-fallback question; checking found no `OPENAI_API_KEY` configured, so no API generation was performed. Do not treat partial rollout as completion. Continue remaining ten wardrobes/three building sheets and repair Đinh trained armour when generation is available; follow the doc's final coverage gate. No automation scheduled.

## 2026-09-06 — Đông Hồ continuation: 229/234 active

- Built-in ImageGen became available and produced 13 further reviewed masters: all ten remaining wardrobes (Lê, Trịnh, Nguyễn Lords, Tây Sơn, Nguyễn, Song, Yuan, Ming, Qing, Champa) and all three building sheets. Added 178 active entries. Current coverage: 190/195 soldier slots, 11/11 settlements and 28/28 accepted buildings. There are 234 exported PNGs; the five Đinh trained drafts remain unapproved and their original armoured sprites remain selected.
- Preserved original weapons, era silhouettes, hats and protective coverage while simplifying pigment and ink. All new references and generated masters were visually inspected. Exact continuation prompts and generated-file provenance, including the failed final repair, are saved in `docs/dong-ho-style-v4-generation-log.json`. Built-in generation only; no API key was configured and no CLI/API generation was performed.
- Fixed tiny opaque checker remnants inside fence rails and grain-bin stair gaps: structure matte extraction now clears small enclosed neutral regions, while army extraction keeps small weapon highlights. Re-exported buildings-1 and citadels and inspected dark-background comparisons. No programmatic redraw/recolour substituted for generated art.
- Fixed offline asset classification so versioned conquest sprite directories stay optional. Production TypeScript/Vite/SW build passes; all 234 v4 PNGs plus their manifest are optional and none block the shell. Existing font-at-runtime and bundle-size warnings remain.
- Verification: 234/234 runtime textures load, 229 reviewed paths selected; every export passes pixel/alpha/edge/magenta and canvas checks, ranged aliases identical. Settlement layouts 11/11 with ten improvements each; zero live overlaps among 42 labels and 35 structures. New Nguyễn/Qing map/battle animation 13/13; History 9/9 across 105 soldier configurations and 35 formations. Low/medium/high live/cached structure audit passes including culling/pan restoration and no duplicate vector ink. Ground order 5/5 at actual high quality (corrected its storage key). Missing/corrupt settlement fallback passes.
- Required game client screenshot and text state inspected in `output/web-game/dongho-full-pass-client/`. Captured medium maps at 1.2x/2.4x, seven supported History wardrobes and Lê/Ming battlefield in `output/dongho-style-v4/game/`. The first battle capture inherited the still-map accumulator freeze; corrected the capture setup to restore the clock and wait for actual movement. Final samples advance x=90.17 -> 113.21 with frames 1,0,2,3; no browser errors. Selected final screenshots and comparisons copied into `docs/dong-ho-soldiers-buildings/`.
- Updated `docs/dong-ho-soldiers-buildings.md` and `docs/30-dong-ho-visual-language.md` with current coverage, evidence, prompts and remaining work. The original 6/10 rating remains a pre-change baseline; no whole-game rescore or physical-device performance claim.
- Remaining blocker: the final targeted Đinh trained armour edit returned a new `usage_limit_reached`, with reset reported as 2026-09-05 21:29:03 UTC / 2026-09-06 04:29:03 Bangkok. When available, use the current v4 master as edit target and original Đinh reference for the trained chest/shoulder/skirt coverage; preserve accepted levy/royal rows. Inspect, then remove the exporter hold, re-export and verify 234/234 active. Do not declare full consistency until those five slots pass. No automatic retry scheduled.

## 2026-09-06 — Đông Hồ map and final soldier rollout complete

- User request: continue until finished, including all tile-map assets (mountains, trees, fields, farmers, buffalo, carts and remaining families), consistently expressing Đông Hồ style.
- Built-in ImageGen succeeded for the targeted Đinh trained armour repair. Reviewed restored chest/shoulder/skirt protection, exported all fifteen Đinh slots and cleared the final five holds. Soldier/structure coverage is now 234/234: 195 soldier slots (156 unique plus 39 ranged aliases), 11 settlements, 28 accepted buildings. The catalog-rejected mine-worker is excluded.
- Generated and reviewed thirteen map masters: four seasonal plant sheets, seven mountains/ridges, five paddy systems and a bridge, eleven life/season accents, twenty-three markers/flags, and four four-frame walking sheets. Exported all 91 map entries with true alpha. Registry now selects all 321 accepted still assets plus four walking sheets (325 total).
- Mechanical map exporter preserves complete connected silhouettes, source canvas contracts and transparent borders. Cleared checker remnants inside cart wheels, the traveller's arm, coin and sun-ring centres while retaining cream pigment and snow. Walking sheets share measured anchors and baselines; actual feet/hooves change pose. No scripted painting or recolouring substituted for ImageGen drawing. Actual prompts/provenance saved in docs/dong-ho-map-v4-generation-log.json and docs/dong-ho-dinh-armour-repair.json; no API generation used.
- Matched code-drawn shrubs, water, roads and bridges with flat pigment and restrained carved marks. Increased paddy-plate opacity. Screenshot review caught cached foliage blurring against sharp buildings: medium/high now keep nearby foliage and relief live with view culling, while low and cheaper adaptive settings retain caching. Fixed new transparent-padding drift by sorting scatter at its measured ink base.
- Verified all 91 exported map PNGs and 325 selected/loaded runtime entries; soldier/structure audit 234/234; walk sheets 17/17; actual movers 22/22; Đinh/Song map and battle animation 13/13; natural terrain and markers 18/18; ground order 5/5 (165 relief and 6847 scatter images at zero base drift). Low/medium/high bake/culling checks pass, including offscreen/return and no duplicated vector buffers. Missing and corrupt settlement and all 91 map-image responses recover through procedural drawing; fallback screenshot inspected.
- Captured twelve actual medium map views across all four seasons (country 1.2x, village 2.4x, fields 1.8x); reviewed foliage, cutouts, walk frames and gameplay captures. Required game-client screenshot/text state inspected under output/web-game/dongho-map-final-client. Browser error logs empty on normal loads. Production TypeScript/Vite/SW build passes, with existing font and bundle warnings; versioned art remains optional offline.
- Updated docs/dong-ho-soldiers-buildings.md and docs/30-dong-ho-visual-language.md to remove obsolete holds. Added docs/dong-ho-map-style.md with coverage, evidence, exact/normalized prompts and selected comparison/gameplay images. Runtime art is in public/art/conquest-dongho-v4; original references, accepted masters, full reviews and audits remain under output/dongho-map-v4 and output/dongho-style-v4.
- Requested soldier, building and map-tile coverage is complete with no pending entries. Broader limits are documented: ground/fog caching can soften on enlargement, History enlarges small animation sprites, and portrait/menu systems are separate. No physical-phone performance measurement or whole-game rescore claimed.

## 2026-09-06 — quieter mountain palette

- User disliked the colourful mountains. Used built-in ImageGen to edit only the palette of the existing seven-variant master: warm limestone greige, muted slate and a little dusty sage, retaining contours and silhouettes. Saved generated exec-64d19084-5286-4fc8-8c39-beaa2f08bc6e.png as output/dongho-map-v4/masters/mountains.png; original colourful master and all seven previous runtime exports are retained in output/dongho-map-v4.
- Exported all seven revised terrain sprites to their existing v4 runtime paths. Updated matte extraction to preserve enclosed neutral stone pigment; visually reviewed paper/dark boards. Updated future mountain prompts so the bright palette will not return on regeneration. Exact edit prompt and provenance: docs/dong-ho-mountain-palette-edit.json.
- Reduced only mountain seasonal tint strength from 35% to 12%; all four seasonal cues remain, now subtle. Other art families keep their palette. Coverage remains 91/91 map entries and 325 total selected assets.
- Verified 91 map export checks and all 325 loaded runtime entries; natural terrain/marker/season checks 18/18; actual game screenshots and required client screenshot/text state inspected. Production TypeScript/Vite/SW build passes with existing warnings. Documentation and selected gameplay/comparison images updated in docs/dong-ho-map-style.md. No remaining task-specific work.

## 2026-09-06 — mountain colour correction after user feedback

- User explicitly rejected the muted version as too close to black and white and requested another Đông Hồ generation with natural mountain colours. Regenerated the seven-variant master with built-in ImageGen: pale ochre limestone, soft moss-green vegetation and muted blue-green rock faces, retaining the existing flat pigment, silhouettes and dark carved contours. Removed red/orange bands without removing colour.
- Current generated file: exec-2e718854-166c-4147-8916-868d22e2ec0d.png, copied into output/dongho-map-v4/masters/mountains.png and exported to all seven existing public/art/conquest-dongho-v4/terrain paths. The grey master and exports are retained as mountains-grey-rejected.png and mountain-grey-rejected/. The historical grey prompt record is marked superseded; current exact prompt/provenance is docs/dong-ho-mountain-natural-colour-edit.json. Future mountain prompts now explicitly require three visible natural pigment families and prohibit greyscale treatment. Seasonal tint stays at the restrained 12%.
- Rechecked all 91 map exports and 325 loaded asset entries; seasonal/natural terrain checks pass 18/18. Inspected paper/dark cutouts, actual spring/autumn mountain views, and required client screenshot/text state in output/web-game/dongho-mountain-natural-client. Normal-load browser logs have no errors. Production build passes with existing font/bundle warnings. Updated documentation and previews to the coloured version. Unrelated concurrent ConquestScene edits were left untouched.

## 2026-09-06 — ground colour revision toward the print sheet

- User asked whether the background tile colours needed updating for Đông Hồ consistency, then to try the update. Researched the tradition: five unmixed pigments printed as flat blocks on điệp, a ground that is coloured only as one whole-sheet tone (vàng cái, nền gấc), and gradation within a colour belonging to Hàng Trống hand-tinting rather than woodblock printing. Measured the medium captures: owned ground in summer/autumn had compounded to 70% lightness (`#bbcc99`, `#ddbb88`), the value of the karst highlights and the hue of the roof ochre; reference prints sit at 83–90%.
- Colour-only changes, all derived from existing pigments: new `PIGMENT.diepWarm` (điệp 0.3 toward hoePale) as the ground fill in every season; plains lean on foliage 0.25 → 0.08; forest floor toward nâu 0.12 instead of foliage 0.3; summer/autumn ground casts 0.14/0.15 → 0.05/0.06; cropland cast 0.55 → 0.4 of paddy alpha; the summer/autumn/winter `ground` and `groundRelief` values the menu diorama reads were re-pitched to match. Files: `src/ui/ink/palette.ts`, `src/ui/ink/season.ts`, `src/ui/DongHoMapRenderer.ts`.
- Verified: `tsc` clean and `yarn build` passes with the existing warnings; natural terrain/marker/season checks 18/18; sixteen seasonal captures re-shot (`shot-dongho-map-v4.mjs`) with no browser errors and inspected — owned ground now `#eeddbb` (83%) in spring/summer/winter and `#eeddaa` (80%) in autumn; autumn menu diorama inspected. Before/after boards and a dated section added to `docs/dong-ho-map-style.md`.
- Not done, by scope: the ground is still overlapping radial tones. Flat torn-edge terrain blocks, a flat water block with one indigo contour, and confining the ownership hatch to borders are treatment changes, listed in the doc section as the next step.

## 2026-09-06 — armies march the drawn roads

- User asked for army movement on the map to follow the roads and be more realistic. Marching hosts built a curve of their own (`army|from|to`, seat centre to seat centre, offset 18 across and 28 up) and cut across country beside the painted road; carts keyed `farm|city` unsorted rode a mirrored meander whenever the farm id sorted after the city's.
- `drawnRoadBetween` in `src/map/roadCurve.ts` reproduces the one drawn road for a pair (sorted key, gate entrances, 11-unit runway) and says whether it is walked backwards; `TrafficRenderer` roads, carts and travellers all go through it. New `src/map/marchRoute.ts`: an arc-length `MarchRoute` chaining stand → gate → road → far gate → far stand, and `reversedSpline` (a uniform Catmull-Rom is reversal-symmetric, so the reversed point list is the identical road; swapping the endpoints mirrors the bow). `ArmyRenderer` walks that route with the marker exactly on the road, files the column along the road's own tangent read a little ahead and clamped to the road, kicks dust back along the tangent, and when an order ends walks the host home — or forward onto a province the tick already granted — back along the same road at marching pace instead of a straight slide. A `resting` set stops the next refresh from snapping a walking host to its stand, which the old rest branch did. Where no road is drawn the host makes a `track|lo|hi` on the same sorted key.
- Verified: `tsc` and `yarn build` pass. New `test_scripts/verify/verify-march-roads.mjs` 16/16 — on the road to 0.0 units over 20 samples, filed along it 20/20, fastest step 87 u/s, a stopped clock holds the column, arrival at the far stand, a cancelled march retraces the road monotonically over 15 samples, carts within 3 units of their drawn road; `verify-conquest-army-animation.mjs` 13/13; frames inspected under `output/march-roads/`. Map skill doc updated.
- Harness traps: a host without a standing order belongs to the Ascent autopilot, which marches it back to the capital the tick after it arrives — issue test marches through `setArmyOrders({ kind: 'defend' })`; a one-tick leg can resolve before the host is a third of the way along, so the cancel test freezes `ascentAccumulator` as the map shooters do; the marker signature stores the heading to two decimals, so compare compass indices.
- A host on the road is drawn in column whichever way it walks: the walk home is filed along the road facing home (heading read behind the host, negated), the general's portrait only returns at the stand, and a walk that ends between refreshes redraws its host at once so the column re-forms beside its seat rather than at the next tick.
- Not changed: the standing spot beside the seat sits inside the compound's upper right, so a host's first step to the gate crosses the walls; the ground, water and hatch treatment items from the colour round remain open.

## 2026-09-06 — marches hold the line, keep the clock, and keep formation

- User reported three things after playing: a host that claimed or attacked a province marched there and straight back while the fight was still on; the march animation did not take the seasons the order said; and a moving host should keep its close standing formation on the road rather than string out in file.
- **Picket.** `ArmyRenderer` now reads the fight a host has gone to (`engagementLand`: a live watched battle it is enrolled in on another province, or an intimidation it is applying) and holds the host at the province line of the road to it — walking out to the line at leg pace if it opened the fight from its own stand — facing the fight, feet still, until the engagement ends; only then does it walk on (province taken) or home. A host with no order and away from its stand walks its road home in formation, facing home, and re-forms beside its seat the moment it arrives.
- **Clock.** Both scenes expose `tickPhase()` (accumulator over tick length) and write `state.tickPhase` every frame and just past each boundary. A fresh march starts its tick count at `-tickPhase` (`marchStartProgress` in `WarSystem`), so a march of x seasons takes x seasons of wall time and resolves on the boundary after them instead of banking most of a tick at the next boundary however late it was ordered; the war board rounds the remaining seasons up. The renderer records the phase a leg was drawn at, times the walk to what is left on the clock, re-times it on every refresh past a 3% drift, and marches linearly so the men reach the far stand on the tick.
- **Formation.** `HostKit.column` (off) gates the file plan in both draw paths; a host on the map keeps its close arrangement standing or walking and only turns to face the road.
- Verified: `tsc`, `yarn build`; `verify-march-roads.mjs` 21/21 (claim marches out on the road, holds still at 0.50 facing the fight, walks home on the road when called off; march starts at the host's feet, on the road 25/25, faces it 25/25, fastest 155 u/s, marker at the far stand within 300 ms of the state's arrival; cancelled march retraces the road); `verify-conquest-offer.mjs` 9/9; `verify-conquest-army-animation.mjs` 13/13; smoke 18/18. Frames inspected under `output/march-roads/` (picket, gate, road, far gate, arrival, walking home). The long rendered runs (`verify-ascent`, `verify-empire-fairness`) were not re-run.
- Traps: `ConquestScene.ts` is CRLF while the rest of this patch set is LF, so anchored edits must convert; the earlier picket patch left four LF lines in it, since normalised. A sine ease-out on the last leg put the marker at the far gate half a second before the state did — marches are linear now.

## 2026-09-06 — the wealth factor: prices that scale with what the realm holds

- User report after playing: card and action costs did not scale with the resources held, so runs ended with a lot of gold, food and goods left over and resources felt useless; asked for a little more economic challenge, measured in real play rather than assumed.
- **Measured first** (`diag-economy-probe.mjs`, 8 seeds × 600 ticks, three drivers): the naive spender peaked holding 15 seasons of gross income, the land-reading driver ended holding 14.5 (7.4k gold at death), the careful steward ended holding 59 (12k gold at death, peak 23.8k) — routine prices were tens to hundreds of coin against piles of thousands, grain and goods had no scale at all, and the only four-figure sinks were war cards that escalate 1.4× per purchase until refused.
- **Shipped.** `priceScale.ts` now multiplies two smoothed factors: the existing income scale and a per-store wealth factor — seasons of the realm's own gross (gold) or use (grain, goods) held above `PRICE_WEALTH_FREE_SEASONS` 4, to the half power, capped at 4, with the founding's stores exempt (`PRICE_WEALTH_STORE_FLOOR` 600). Gold prices wear both; grain and goods prices wear their own store's factor (`storePriceScale`, `scaledCost`). Applied to buildings and upgrades, bribes and diplomacy, musters (grain and iron now scale with the granary and armoury), refits (equip, drill, reinforce), rerolls, restore bills, and wonders (were a flat few hundred). Response, rival and envoy cards quote a share of the treasury as a floor: walls 18%, sellswords 28%, buy-off 35%, tribute 30%, pact 25%, gifts 5%; per-purchase war escalation 1.4 → 1.3. Graft line 25 → 12 seasons of income; stores rot above 12 seasons of use instead of 20. The autopilot's build reserve reads the income scale alone so a hoard cannot defend itself against being spent. `wealthScale` is optional on the save.
- **Measured after** (16 seeds): end-of-run gold, mean — steward 12.0k → 4.5k (median 4.3k), landwise 7.4k → 3.0k, engaged 0.7k → 2.1k (median 1.4k); steward peak 23.8k → 16.6k, mean 10.8k → 6.3k; food/goods end in the low hundreds for the land drivers. Funscore 85/85 at 16 seeds, agency 2.17 (engaged 21.3 waves vs declining 9.8); economy dimension 1× oversupply. A second pass (walls 15%, sellswords 25%, graft 8%) measured worse on every number at 8 seeds and was reverted — the game is noisy enough that eight seeds mislead; judge on sixteen.
- Gates: `verify-setup-phase` 16/16 (founding still pays base price; grossing ×5 pays 2.61×), `verify-economy` green, `verify-modes-regression` clean (every new scale is exactly 1 outside Dragon Ascent, so the classic paths are byte-identical by construction); `tsc` and `yarn build` pass.
- Traps: writing any repo file while a rendered harness is booting destroys its page context — another session was adding story prints during this round, which also surfaced `story-print:*` image console errors in probe runs that are not from this change. The war cards' 6-seasons-of-income base with escalation makes a late wall cost more than a rich treasury holds; that is the designed rarity, not a bug, and the wealth factor does not touch it.

## 2026-09-06 - Fifty new Dong Ho card prints (in progress)
- User requested at least 50 new images matching public/art/story-prints, applied to powers and some stories.
- Exactly 50 existing powers each receive an individual built-in ImageGen scene using the original three prints as references. Generation is ongoing; prompts and per-image provenance are saved in docs/dong-ho-card-print-*.json.
- Added full power mapping, illustrated founding choices, and 16 exact story-fragment mappings with original portraits and optional-art fallbacks. TypeScript and scoped whitespace checks pass.
- Remaining: finish generation, inspect all images, run card/story phone QA and build, complete docs/dong-ho-card-prints.md. Avoid unrelated concurrent workspace changes.

## 2026-09-06 - Fifty new Dong Ho card prints completed
- Completed 50 individual built-in ImageGen scenes, each referencing the original harvest/muster/petition prints. All 50 PNG masters, exact prompts and provenance records are retained; 576x384 WebP exports total 3,552,736 bytes. The runtime folder now contains 53 prints.
- Applied one unique scene to every power ID, including founding choices, plus 16 active Chronicle fragments. Replaced the retired nam-quoc whisper mapping with river-stakes.his-men-would-cut-the-timber. Existing game rules and cast portraits remain intact.
- Verified all generated images, five review boards and all 50 in-game card bakes. Dedicated phone QA passed 49/49 with zero browser errors: Vietnamese 390x844, English 390x620, all selected stories, opening choices, scrolling/pointer input and missing-art fallbacks. Required bundled web-game client passed; screenshot and text state inspected. Production build and scoped whitespace checks passed. Service worker contains 53 optional story prints, none install-critical.
- Gallery and final evidence: docs/dong-ho-card-prints.md. Screenshots/results: output/dongho-card-prints/verification. No remaining work for this 50-print request.
- Harness notes: optionCard hit targets are Rectangles, not Zones. Missing-art tests rename texture keys rather than destroying textures that an outgoing frame may still reference; clear the pending prompt before closing an overlay because closeOverlay refreshes and reopens pending prompts. Reuse real story options to keep test screenshots localized correctly. Preserve unrelated concurrent edits.

## 2026-09-06 - Dong Ho menu colours and battle clarity completed

- Request: keep the menu while matching the new Dong Ho colours; improve battle styling and icon clarity. Menu layout, image sources and animations retained; shell-paper background/feathers/veil, stronger existing mountain/bamboo opacity, indigo tagline and quieter shared secondary surfaces now match the card family.
- Reworked code-native formation symbols and enlarged dock glyphs to a fitted maximum of 23 units. Added stance symbols, ochre selected tiles, a current-shape tick, and directional one/two-mark matchup hints. Opening and difficulty visibility rules remain intact. Troop/person and morale/heart rails now use clear flat pigment fills; four resource SVGs use the common palette. Battle hills use muted indigo/green, camps ochre cloth, and authored plants retain more pigment. No simulation or army movement changes.
- Added optional custom printed tile colours to InkUI using cached stamps; the battle gauges retain one Graphics object. The softRidge fill override defaults to its original colour for every existing caller. Preserve all prior/concurrent edits in InkUI/props and elsewhere.
- Verified: dedicated vi/en 390x620/844 DPR2 visual fit and difficulty-state checks 25/25; battle feedback including actual pointer formation change/transit/arrival/Moments 25/25; dock 19/19; menu utility/navigation/languages 17/17; battle dials/stamina/telegraph 13/13; arena setup 7 assertions all pass. Browser errors: none. Build and scoped diff whitespace checks pass; existing font/chunk warnings remain.
- Required web-game client run before and after; screenshots/text states inspected. All four combat captures plus short and tall menu captures inspected. Evidence/gallery: docs/dong-ho-menu-battle.md and docs/dong-ho-menu-battle/. No outstanding work for this request. Local preview server started on 5182 (exec session 93608).
- Harness notes: transit-bar detection now checks its active visible nonempty drawing directly, since the new selection tick leaves as that bar enters (total Graphics count can stay constant). Dedicated captures stop MenuScene before arena start so render_game_to_text reports the game; progress through actual contact and answer transient Moments before inspecting the ordinary dock. These are presentation fixtures; existing feedback checks exercise real pointer commands and temporary decisions.

## 2026-09-06 - Menu plates repainted in Đông Hồ pigment

- Request: "apply this resources to the menu" with three 1536x1024 prints — the bamboo grove, the karst range and the river-and-paddies ground — repainted in the card family's pigments (chàm river and mountain shadow, lá xanh foliage, hòe paddies, a plain sheet). They are new drawings of the same composition, not recolours of the old plates (bamboo overlaps its v1 silhouette by 31%, the mountains by 68%), so nothing about the old alpha could be reused.
- The prints reached the session as JPEGs flattened onto black, so the two transparent plates were rebuilt: background = the near-black region reachable from the frame border plus any true-black pocket of 12+ pixels enclosed between leaves; only the two-pixel ring against it is keyed and un-premultiplied, so the print's own black ink stays opaque. A shape-based pocket rule (dark blobs surviving a 7x7 erosion) was tried and rejected — it punched white holes through the thick ink at the culm bases and on ridge lines, while the dark patches it targeted in the canopy are the print's shading. Tool: `test_scripts/scratch/rebuild-menu-layers.mjs`; the grove is shown at one fifth scale so the remaining edge noise is sub-pixel. Dropping the original PNGs over `public/art/menu-layer-{bamboo-v2,mountains-v2,ground-v5}.png` replaces the reconstruction with nothing else to change.
- Code: `PreloadScene` loads ground v5 / mountains v2 / bamboo v2 / lotus v2; `MenuScene` re-measured the grove's ink bounds (519,358 → 1535,1007; the v2 grove is 1016x649 against v1's 904x522), remapped the two wind crops to it, moved the grove anchor 1400 → 1380 so its wider canopy no longer runs past the overscanned right edge on the 844 sheet, artwork version 10, culm count 13. `verify-living-map.mjs` pins the new textures and its "upper part of the plate is clear" guard is 30% rather than 40% for the taller grove; `scripts/build-share.mjs` draws the share card from the new ground and mountains.
- Verified: `verify-living-map.mjs` ALL PASS on 844 and 664 with no console errors (bamboo bounds, transparency corners, wind layers, mist, water order, plus the unchanged map contract); `npx tsc --noEmit` clean; before/after captures at vi/en 390x620 and 390x844 in `output/menu-plates/{before,after}/`, inspected. After the anchor nudge: `verify-menu-fit.mjs` all 24 theme/language/height combinations with no text overlap; `verify-menu-icons-flags.mjs` 17/17; `verify-living-map.mjs` ALL PASS again; `yarn build` clean (service-worker shell 73.08 MB, 51 shell + 1018 art files).
- The lotus print came in a follow-up message with a *baked* checkerboard (alpha 255 everywhere, 18 px squares at grey 254/240 under a whitish haze — the image tool's fake-transparency output, seen before on the farm plates). It is a redraw of the v1 lotus (76% silhouette overlap, a little larger), so borrowing the v1 alpha lost 27k px of new ink and was rejected; the shipped `menu-layer-lotus-v2.png` is keyed instead: background = near-neutral light pixels (channels within 7, luminance ≥ 226) reachable from the border, plus enclosed neutral patches of 40+ px that show both checker levels (the pockets the ripple rings close off — five of them); only a 2 px ring is keyed by distance from the local background and un-composited against it, so cream petals (warm, not neutral) and the ink survive. Tool: `test_scripts/scratch/rebuild-menu-lotus.mjs`. Stem/waterline anchors (0.19 / 0.345 across, 0.915 / 0.902 down) still land on the new stems, so the wakes and idle swells were left alone. Artwork version 11.
- "Please use previous background color" (with a desktop screenshot): the body and canvas colours were unchanged, so the background that had changed was the ground print's own paper — v4 carried a warm mottled sheet (#ead0a0), the new print a smooth pinkish cream (#f4e1c0) that read as a pasted block against the page. Re-papered `menu-layer-ground-v5.png` in place: wherever the print is that cream (sky, far bank, sandbar) the v4 plate's pixel is taken back, with a 12-step ramp so the drawing's edges stay put (`test_scripts/scratch/repaper-menu-ground.mjs`; 830k px replaced). In the menu the plate sky now measures #ebd5ac against #ead4ab before the round. The cream original is kept only in the session scratchpad.
- Verified after the lotus and the re-papering: `verify-living-map.mjs` ALL PASS, `verify-menu-fit.mjs` all 24, `verify-menu-icons-flags.mjs` 17/17, `npx tsc --noEmit` clean, `yarn build` clean (73.54 MB shell).
- Not done: the twelve older menu plates (`menu-layer-ground-v1..v4`, `farm-v1..v4`, `farmstead-v2`, `bamboo-v1`, `mountains-v1`, `lotus-v1`) are all still in `public/art/` and, since only `share/` and dotfiles are skipped, all install-critical in the service-worker precache — about 16 MB, none of it used any more. Deleting them is a repo-owner call.

## 2026-09-06 - Menu: the sheet, the range's footing, and the drifting leaves

- Three notes on the repainted front page. (1) "Still menu background white compare with around": the working tree had moved the menu's own paper from `PIGMENT.diep` to the brighter `PIGMENT.diepHi` (paper rect, the top veil, and the illustration's four feather gradients) to match the card stock. The shell paints `diep` behind the canvas in `index.html`, so on any letterboxed window — every desktop one — the page read as a white panel laid on the darker shell. All six sites are back on `diep`; the canvas sheet and the shell are one colour again. Card prints keep their own brighter ground.
- (2) "Move mountain in main menu down to connect with below": the range and the valley are separate sheets in one registration, and at that registration the karst feet stop at 0.49 of the frame while the ground's far bank starts at 0.455, so bare paper showed between the peaks and the range read as a strip pasted above the valley. New `MOUNTAIN_DROP` (0.035 of plate height, 36 source px) drops the mountain plate; its drift tween now swings around that home instead of the plate centre, and the five mist wisps move down with it so they no longer whiten the join. Offsets 20/36/52 were composited and compared: 20 leaves a seam, 52 buries the far paddies.
- (3) "Make color of leaf consistent": the drifting leaves were pitched to the old sage pair (`tram`, `tramPale`) with veins and outlines in `tramDeep`, and `tramPale` was lighter than anything on the repainted page. They now read off the prints — deep pad green `mix(tram, tramDeep, 0.55)` = #6a7d51 against the lotus pads' measured #607842, the mid `tram`, and one dry `mix(hoePale, hoe, 0.45)` = #cfa75a against the bamboo sheaths' #c29f5c (those are the pigments; a blade paints at 0.94 alpha over the sheet, so a patch of rendered leaf samples lighter) — each closed with a `muc` ink line, which is what every leaf on those sheets has.
- Verified: `verify-living-map.mjs` ALL PASS, `verify-menu-fit.mjs` all 24 combinations, `verify-menu-icons-flags.mjs` 17/17, `npx tsc --noEmit` clean, `yarn build` clean. Captures: `output/menu-plates/after/` (vi/en 390x620 and 390x844, a 1100x900 letterboxed desktop shot for the sheet, and `leaf-tones.png` with one leaf of each tone parked side by side).

## 2026-09-06 - Menu: the dynasty board's columns, and a range that stops floating

- Reported: text overlapping on the front page's dynasty board, and the mountains still reading as if they hang in mid-air.
- **The board.** Two faults, both in the right column. The seal's caption is right-aligned at `width - 14` but only 52 units were reserved for it — enough for "7 to choose", not for "còn 7 lượt", so the reign line printed six units under the caption (fourteen in English). The reserve is now the caption's own measured width plus its margin and a gutter. That alone leaves the words about 105 units when a founder portrait is also on the board, which the reign line can only reach by shrinking below four fifths, so the portrait now gives way when the column would fall under 118 — the same rule the merged board already followed, one size up. The reign line also no longer wraps: it was wrapped to the column, and 16 units below it is the progress rule, so a Vietnamese line breaking after "lên" printed `Cấp 11` straight through the bar. Wrapping is off and the type scales to the column instead, floor 0.8.
- Measured across language, board height and whether a choice is owed: eight boards, no text overlapping any other, every reign line at full size, and the portrait present exactly when no seal is (`test_scripts/scratch/probe-dynasty-matrix.mjs`).
- **The range.** The mountain plate now prints at full alpha rather than 0.96, and the five mist wisps stopped being sheet-coloured: `diepHi` at 0.72 over `diep` at 0.66 was a white smear laid across the karst feet, which is why the range read as hanging in the air. They are now a chàm-grey band at a third of that weight and about half the height — visible as haze in the valley, invisible as a wash over the pigment. The wisps keep their object alpha and their drift, so the living-map contract still measures what it was written for.
- **Clouds: tried, rejected, removed.** With the sky still bare paper above the range, printed cloud banks were built behind the peaks — a `clouds` container under the mountain plate, four banks drawn as the upper envelope of overlapping half-lobes with a woodblock double contour, drifting a few pixels a minute. Three shapes were needed to stop them printing as pale slabs (the flat base had to go, then the ends had to be tapered by a window, or the outer lobes overran the bank's width and were cut into vertical sides). The result was reviewed and rejected as looking bad, and every part of it is gone: container, banks, painter, metadata and motion keys. Do not rebuild it. One thing it did expose: a bank centred at 0.86 of the plate with a 0.36 width painted past the picture's right edge onto the bare sheet on the contained short-sheet layout, which anything new drawn in that container would have to avoid.
- With the printed clouds gone, the valley haze is the only cloud on the sheet, and it was asked to carry more: the wisps are about a quarter wider and half again as deep (span 0.2 to 0.26 of the plate, band 0.032 to 0.046 of its height). That is a size the earlier sheet-coloured version could not have taken — it is only affordable because the fills are chàm-grey now.
- **A second pass on those wisps was rejected and reverted.** Asked for more height, stronger colour and more speed, they were rebuilt as a lit crown over a chàm belly with an ink line along the top, narrowed so five banks read as five clouds, and given half the crossing time. The verdict was that it stopped looking like cloud at all. Everything about it is undone — fills, ink, sizes, positions, drift, alpha — and the wisps are back to the chàm-grey ellipses exactly as they were. Do not restyle them. The only thing kept is a non-visual `menuMistSpan` on each wisp, which `test_scripts/scratch/probe-cloud-bounds.mjs` reads to check a bank's reach against the plate's edges (a Graphics has no bounds to ask for). That probe reports the outermost wisps overrunning the picture by up to 18 units at their extremes, which is a worst-case estimate — the painted edge is faint enough that nothing is visible outside the plate on the contained short sheet, confirmed by capture. Left alone rather than nudged, since the positions are part of the look that was approved.
- **The range was redrawn.** A new mountain print arrived with the middle of the range fixed — the two massifs now share one continuous green skirt across the full width instead of ending in mid-air either side of the gap. Rebuilt from its black-flattened copy by the same border-flood rule and installed as `menu-layer-mountains-v3.png` (`test_scripts/scratch/rebuild-black-plate.mjs`, the single-plate form of that tool). Its geometry matches v2 closely — ink from y=56 to y=503 against 68 to 503 — so `MOUNTAIN_DROP` still lands the feet on the far bank and nothing else moved. Artwork version 12. `menu-layer-mountains-v2.png` is now unused and still in the repo.
- **The lotus stands in the river now.** On its own registration the stems ended at 0.95 of the frame, which at the near-left of the valley is still the pale sandbar rather than the water — the flowers read as growing out of the bank. New `LOTUS_SINK` (0.035 of plate height, the same magnitude as the range's drop) sinks the plate past the bar's edge into open water; the ambient and pointer motion now settles around that home rather than the picture's centre, and both stem anchors carry the offset so every ripple, wake and idle swell still starts at a waterline. The living-map bands for those two were widened to match (wake 0.88–0.98, idle swell 0.86–0.99).
- Verified: `verify-living-map.mjs` ALL PASS, `verify-menu-fit.mjs` all 24 combinations, `verify-menu-icons-flags.mjs` 17/17, the dynasty matrix 8/8 with no overlapping text, `npx tsc --noEmit` clean, `yarn build` clean. Captures in `output/menu-plates/after/`: `tablet-{vi,en}-{844,620}-{owed,clear}.png` and the phone/desktop menus.

## 2026-09-06 - Approved river app icon
- User selected the boat, stakes and waves concept and requested Android, iOS, web, menu and generated store branding updates.
- Preserved the approved master; generated real-alpha foreground and monochrome derivatives. Added shared exports, adaptive/themed layers, iOS tinted artwork, favicon/ICO/PWA variants and an isolated native-export package.
- Menu and loading emblems now use the river artwork; store feature/footer and social card sources updated. Visual and integration verification is in progress; records live in docs/design/game-icon-v7.


- Complete: all five requested surfaces now use the approved boat identity, including menu/loading, native adaptive/themed/tinted assets, web icons, both store icons, feature art, 19 screenshot frames, generated store notes/config and social card.
- Verified 20 PNGs plus native density resources/config, safe areas, sRGB, real alpha and ICO/manifest references. All 24 menu-fit cases passed; game client screenshot/state reviewed. TypeScript, direct Vite web/shell builds, service worker and mobile sync passed; Yarn build shortcut lacked a tsc shim, so existing scripts were invoked directly without installing packages.
- Pack: docs/design/game-icon-v7/van-thang-river-icons.zip. Device installation, APK/IPA build, web deployment and store upload were not performed.


## 2026-09-06 - Transparent browser favicon
- User rejected the simplified V8 favicon and requested the previous detailed boat with transparency. Browser exports now use the existing V7 transparent foreground; no new drawing was generated for this correction.
- Refreshed 16/32/48/96 PNG, ICO and SVG exports, fresh V9 browser URLs, portable icon pack and light/dark actual-size comparison in docs/design/game-icon-v9. Native, menu, store and installed PWA sources are unchanged.
- Export drift check, 20-PNG/platform validation, production Vite build and service-worker generation passed. The 16 px transparency check accounts for antialiased edges while requiring clear corners and genuine alpha. Rejected simplified artwork is archived in docs/design/game-icon-v8.


## 2026-09-06 — special historical hero portrait review

- Latest request: check every special hero for Vietnamese historical grounding and attractive Đông Hồ presentation. Audited 143 entries across authored real heroes, historical summon figures and two named teachers. Inspected all eight portrait sheets, plus final refinements on sheets 7–8.
- Added v2-only historical profiles and independent face assembly. Alternate titles share a face/outfit; rarity and game role no longer change anatomy, costume, age or historical insignia. Player-created monarchs, source hero data and older art versions remain intact.
- Corrected Lê Quý Đôn / Đoàn Thị Điểm to the Lê wardrobe and Thường Chiếu to Lý; deliberate youthful portraits for Trần Quốc Toản / Lê Ngọc Hân; clean-shaven Lý Thường Kiệt / Lê Văn Duyệt; restrained monastics without random ritual marks. Named people no longer get arbitrary badges, scars, earrings or face tattoos.
- Inspected museum photographs of Hoàng Hoa Thám and the museum-published Nguyễn Thiện Thuật portrait; inspected the Phan Đình Phùng print attributed to a 1935 publication. Recorded evidence limits instead of claiming all images are authenticated lifetime likenesses. Existing PNG assets are reused, atlas size unchanged.
- Fixed the brow-width fitting prefix (actual IDs are brow-, not brows-). Added historical review and complete per-entry inventory under docs/research. Generated searchable offline gallery at output/hero-dongho-v2/special/gallery.html, with actual cached game portraits and interpretation notes.
- Validation passed: special-hero coverage/parts, aliases, all four rarities and role changes, life stages, source immutability, gallery search/period filter/notes/image loading; 367 outfits; 10,304 head/hat/eye fits; 53,232 creator combinations with actual controls; all three art loaders, cache reuse, saved-look preservation and gameplay/roster screens. Web-game skill smoke screenshot and state inspected. Production build passed with existing font-path / bundle-size warnings.
- No unresolved rendering failures found in this review. Historical limits remain explicit: most individual faces/cuts are period interpretations, early figures have especially sparse evidence, and informal royal portraits are not reconstructed grand-court uniforms. Future historical evidence can refine profiles without changing gameplay data.


## 2026-09-07 — Artwork-preserving performance implementation
- Implemented the approved virtual lists, local map caches with shared preparation budgets, lossless atlases and lazy loading, referenced portrait eviction, launch-only Auto selection and elapsed-time frame pacing. Preserved unrelated working-tree changes; this work does not alter gameplay rules, saves or shell origins.
- Codex, shared lane rows, History, Guide and Cabinet collections use stable keyed virtual holders, cached variable-height measurements, clipping and existing flick/tap protection. The 127-row Codex now opens in 69.3 ms at 4× CPU / Medium / DPR 3, mounting 11–14 rows; 26 draw calls and 187 KiB uploads versus the measured 280 / 2,798 KiB baseline.
- Ground and fog use 512-world-unit padded chunks with non-overlapping interiors, visible-first replacement, cancellation, conservative fog and a shared 64/96 MiB color-texture budget including replacements. Idle queues no longer reserve preparation time. Saved RenderTexture disposal releases GPU textures; missing and stale offscreen fog stays concealed during replacement.
- Seasons retain settlement structures and authored scatter images. One real Spring-to-Summer change produces one ground invalidation and one culling-registration pass. Full-bounds spatial registrations skip unchanged objects. A completed farm rebuilds one of 12 cached ground chunks. Latest ordinary fixture: maximum 11.6 ms chunk slice and 7 ms scene-preparation slice; the two gameplay ticks and UI dispatch themselves take 82 ms.
- IMPORTANT remaining limit: the fully revealed 42-land map takes 90.8 seconds to drain preparation under 4× CPU software WebGL, though its largest chunk / scene slices are 14.2 / 13 ms. A diagnostic sees only about 9 actual frames per second and whole-world scatter/culling preparation, so short slices alone do not imply acceptable completion latency or physical-device FPS. Queue completion is recorded separately in the stress benchmark and must be profiled on hardware; frequent state changes could outpace preparation.
- Auto probes High and Balanced for up to two seconds after loading. Clarity retains Medium buffer/text clarity at 30 FPS; softer Low is manual only. Manual choices survive; obsolete adaptive records are removed. Passive measurements affect only the next launch. Default is 60 FPS; full refresh is manual.
- Seven lossless family atlas pages preserve all 326 authored identities, dimensions, transparency and source pixels. Missing download, corrupt decoding and explicit bypass load originals. Map/battle/story families load at scene boundaries. Portrait pressure testing retains live owners while evicting idle entries at the 16 MiB target.
- Verified production build and TypeScript; 9 scroll/input/anchor checks, 10 launch-selection checks, all eight simulated display/cap clock combinations, 11 browser cap checks, 4 simulation-clock checks, shared-budget fairness, six EN/VI phone/tablet/desktop/ultrawide quality-layout cases with pan/zoom and long collections, atlas pixel/loader checks and fog regression. Latest resume/context-recovery suite passes 21/21; Medium map GL gate passes 5/5 with 51 draws / 450 KiB.
- Completed a real-time 20-minute run: 326 turns, 644 battle samples, 39 repeated navigation cycles, no browser errors, chunk budget respected; final reset heap 29.7 MiB (+8.1 MiB from first sample), declining second-half heap. Uses full CDP collection plus Runtime.getHeapUsage, not the stale performance.memory samples from exploratory runs. After the last scheduler change, a separate two-minute check completed 33 turns and four navigation cycles without errors or chunk growth.
- Required web-game client ran against production; screenshot and text state inspected. Map and collection screenshots reviewed across phone and desktop layouts. Reproducible checks, results and remaining hardware/package acceptance are documented in docs/development/performance.md; artifacts are in output/performance-review.
- Remaining acceptance: sustained midrange Android, older supported iPhone and integrated-graphics laptop testing in browser and packaged versions; visual sign-off on those actual displays. No physical-device FPS, total-VRAM budget, or battle-transition latency certification is claimed. The battle harness supports production builds, but whole transition rebuilding remains an optimization opportunity.


## 2026-09-07 — Desktop lag review requested after performance implementation
- User requested inspection and improvement points, not another implementation pass. Fresh production build reviewed in headed Chromium with confirmed AMD Radeon 8060S / ANGLE D3D11 hardware rendering, at 1920×1080 and 3440×1440, DPR 1; source game code unchanged.
- Unthrottled map and Codex held about 60 FPS. Matched 4× CPU revealed/zoomed map comparison: 44.9 FPS and 21.0 ms median CPU; hiding ownership shading only gave 51.3 FPS / 17.6 ms; hiding standing decoration with ownership restored gave 60.1 FPS / 11.7 ms; restoring all art returned 43.6 FPS / 21.1 ms. Hiding art was attribution only, not a product recommendation.
- Confirmed overlooked ownershipTint: 15,639 live Graphics command numbers rendered every frame. High revealed map had 7,473 images. Seasonal bake also made 364 culled decoration objects visible again on ultrawide; reapplying recorded cull state cleared the mismatch. Correct this retention/incremental-culling interaction, but do not claim it alone explains the FPS cost.
- Real hardware season queue completion remains 2.45 s Medium / 2.94–3.03 s High; distinct from the earlier 90.8 s throttled software-renderer measurement. A brief 4× CPU battle-to-power-draft transition produced a 103 ms browser task / 79 ms UI refresh; ordinary updateBattle calls were <=16.9 ms in that small sample.
- Auto with 4× CPU active before launch still picked High (probe CPU P90 2 ms), then delivered 42.7 FPS / CPU P95 28.6 ms on the revealed zoomed-out map. Calibration needs representative desktop content. 1080p allocated a 2702×1520 buffer (~1.98× viewport pixels) because of integer scale rounding.
- Actual Codex wheel input and clipping checked; desktop opening 7.5–15.4 ms with 10 initially mounted / 12 after wheel. Map and Codex screenshots inspected. Full ranked review and caveats in docs/development/desktop-performance-review.md; evidence and isolated production preview in output/desktop-performance-review. Scratch scripts record reproducible tests; no packaged Electron or additional physical device run was claimed.

## 2026-09-07 — Vietnamese royal scroll menu and opening
- Researched Vietnamese sac phong using Vietnam National Museum of History collection photographs/research and the Vietnamese archival association. Implemented a vertical game adaptation: warm paper, curled ends, short wooden rolls, narrow ochre border, cloud flourishes and the existing lotus seal. Sources and historical/design distinctions are in docs/design/royal-scroll-menu.md.
- Kept the approved right-side menu composition and controls. Paper and ornament use one cached stamp per size. Verified 31 menu/navigation checks plus four large/small/low-quality fit cases; the large saved menu fits within a 640-unit sheet, including the rolls. Reviewed EN/VI, compact/wide and installed-client screenshots/text. Production build passed with existing font/chunk warnings.
- Added the user's follow-up opening animation: 140 ms opening beat plus 720 ms downward unroll, with rows revealed after the paper reaches them. Plays once per page load, can be finished by clicking the scroll, and shows immediately for game or OS reduced motion. Redraw/scene shutdown releases temporary objects and restores visibility. First-run tour is queued from actual animation completion.
- Visual verification caught flags reporting empty bounds; their positioned row now supplies the reveal threshold. Replaced a fixed tour delay after checking mismatched tween/timer timing. Opening, midpoint and final screenshots are in output/royal-scroll-menu/opening. Menu navigation, early finish, reduced motion, redraw/restart cleanup and tour ordering checked without browser errors. No outstanding implementation work; motion feel remains available for user review.
